diff --git a/.gitattributes b/.gitattributes
index 1051bd3..a38905c 100644
--- a/.gitattributes
+++ b/.gitattributes
@@ -1,60 +1,58 @@
-[attr]generated gitlab-generated linguist-generated=true
-
 # generated by autotools
-configure generated
-include/config.h.in generated
+configure gitlab-generated linguist-generated=true
+include/config.h.in gitlab-generated linguist-generated=true
 
 # generated by dlls/opencl/make_opencl
-dlls/opencl/opencl.spec generated
-dlls/opencl/opencl_types.h generated
-dlls/opencl/pe_thunks.c generated
-dlls/opencl/unix_thunks.c generated
-dlls/opencl/unixlib.h generated
+dlls/opencl/opencl.spec gitlab-generated linguist-generated=true
+dlls/opencl/opencl_types.h gitlab-generated linguist-generated=true
+dlls/opencl/pe_thunks.c gitlab-generated linguist-generated=true
+dlls/opencl/unix_thunks.c gitlab-generated linguist-generated=true
+dlls/opencl/unixlib.h gitlab-generated linguist-generated=true
 
 # generated by dlls/opengl32/make_opengl
-dlls/opengl32/opengl32.spec generated
-dlls/opengl32/thunks.c generated
-dlls/opengl32/unix_thunks.c generated
-dlls/opengl32/unix_thunks.h generated
-dlls/opengl32/unixlib.h generated
-include/wine/wgl.h generated
+dlls/opengl32/opengl32.spec gitlab-generated linguist-generated=true
+dlls/opengl32/thunks.c gitlab-generated linguist-generated=true
+dlls/opengl32/unix_thunks.c gitlab-generated linguist-generated=true
+dlls/opengl32/unix_thunks.h gitlab-generated linguist-generated=true
+dlls/opengl32/unixlib.h gitlab-generated linguist-generated=true
+include/wine/wgl.h gitlab-generated linguist-generated=true
 
 # generated by dlls/winevulkan/make_vulkan
-dlls/vulkan-1/vulkan-1.spec generated
-dlls/winevulkan/loader_thunks.c generated
-dlls/winevulkan/loader_thunks.h generated
-dlls/winevulkan/vulkan_thunks.c generated
-dlls/winevulkan/vulkan_thunks.h generated
-dlls/winevulkan/winevulkan.json generated
-dlls/winevulkan/winevulkan.spec generated
-include/wine/vulkan.h generated
+dlls/vulkan-1/vulkan-1.spec gitlab-generated linguist-generated=true
+dlls/winevulkan/loader_thunks.c gitlab-generated linguist-generated=true
+dlls/winevulkan/loader_thunks.h gitlab-generated linguist-generated=true
+dlls/winevulkan/vulkan_thunks.c gitlab-generated linguist-generated=true
+dlls/winevulkan/vulkan_thunks.h gitlab-generated linguist-generated=true
+dlls/winevulkan/winevulkan.json gitlab-generated linguist-generated=true
+dlls/winevulkan/winevulkan.spec gitlab-generated linguist-generated=true
+include/wine/vulkan.h gitlab-generated linguist-generated=true
 
 # generated by tools/make_requests
-include/wine/server_protocol.h generated
-server/request_handlers.h generated
-server/request_trace.h generated
+include/wine/server_protocol.h gitlab-generated linguist-generated=true
+server/request_handlers.h gitlab-generated linguist-generated=true
+server/request_trace.h gitlab-generated linguist-generated=true
 
 # generated by tools/make_specfiles
-dlls/ntdll/ntsyscalls.h generated
-dlls/win32u/win32syscalls.h generated
+dlls/ntdll/ntsyscalls.h gitlab-generated linguist-generated=true
+dlls/win32u/win32syscalls.h gitlab-generated linguist-generated=true
 
 # generated by tools/make_unicode
-dlls/dwrite/bracket.c generated
-dlls/dwrite/direction.c generated
-dlls/dwrite/linebreak.c generated
-dlls/dwrite/mirror.c generated
-dlls/dwrite/scripts.c generated
-dlls/dwrite/scripts.h generated
-dlls/dwrite/shapers/arabic_table.c generated
-dlls/gdi32/uniscribe/bracket.c generated
-dlls/gdi32/uniscribe/direction.c generated
-dlls/gdi32/uniscribe/indicsyllable.c generated
-dlls/gdi32/uniscribe/linebreak.c generated
-dlls/gdi32/uniscribe/mirror.c generated
-dlls/gdi32/uniscribe/shaping.c generated
-dlls/kernelbase/kernelbase.rgs generated
-dlls/tzres/tzres.rc generated
-dlls/win32u/vertical.c generated
-dlls/wineps.drv/direction.c generated
-dlls/wineps.drv/vertical.c generated
-nls/*.nls generated
+dlls/dwrite/bracket.c gitlab-generated linguist-generated=true
+dlls/dwrite/direction.c gitlab-generated linguist-generated=true
+dlls/dwrite/linebreak.c gitlab-generated linguist-generated=true
+dlls/dwrite/mirror.c gitlab-generated linguist-generated=true
+dlls/dwrite/scripts.c gitlab-generated linguist-generated=true
+dlls/dwrite/scripts.h gitlab-generated linguist-generated=true
+dlls/dwrite/shapers/arabic_table.c gitlab-generated linguist-generated=true
+dlls/gdi32/uniscribe/bracket.c gitlab-generated linguist-generated=true
+dlls/gdi32/uniscribe/direction.c gitlab-generated linguist-generated=true
+dlls/gdi32/uniscribe/indicsyllable.c gitlab-generated linguist-generated=true
+dlls/gdi32/uniscribe/linebreak.c gitlab-generated linguist-generated=true
+dlls/gdi32/uniscribe/mirror.c gitlab-generated linguist-generated=true
+dlls/gdi32/uniscribe/shaping.c gitlab-generated linguist-generated=true
+dlls/kernelbase/kernelbase.rgs gitlab-generated linguist-generated=true
+dlls/tzres/tzres.rc gitlab-generated linguist-generated=true
+dlls/win32u/vertical.c gitlab-generated linguist-generated=true
+dlls/wineps.drv/direction.c gitlab-generated linguist-generated=true
+dlls/wineps.drv/vertical.c gitlab-generated linguist-generated=true
+nls/*.nls gitlab-generated linguist-generated=true
diff --git a/UPSTREAM-JUICE.txt b/UPSTREAM-JUICE.txt
new file mode 100644
index 0000000..c6389d0
--- /dev/null
+++ b/UPSTREAM-JUICE.txt
@@ -0,0 +1,8 @@
+Upstream: https://gitlab.winehq.org/wine/wine.git
+Mirror used: https://github.com/wine-mirror/wine.git
+Base commit: 6eb2e4c32cc9e271856146df11ed3a5c2cf29234
+Version line: Wine 11.13 development source
+
+This directory contains the complete source plus Juice's iOS changes. The
+audit patch is ../patches/wine-ios.patch. Wine's own COPYING.LIB and LICENSE
+files govern this directory.
diff --git a/configure b/configure
index 73a4d6a..b58136b 100755
--- a/configure
+++ b/configure
@@ -23103,6 +23103,7 @@ wine_fn_config_makefile dlls/winedmo enable_winedmo
 wine_fn_config_makefile dlls/winegstreamer enable_winegstreamer
 wine_fn_config_makefile dlls/winehid.sys enable_winehid_sys
 wine_fn_config_makefile dlls/winemac.drv enable_winemac_drv
+wine_fn_config_makefile dlls/wineios.drv enable_wineios_drv
 wine_fn_config_makefile dlls/winemapi enable_winemapi
 wine_fn_config_makefile dlls/wineoss.drv enable_wineoss_drv
 wine_fn_config_makefile dlls/wineps.drv enable_wineps_drv
diff --git a/configure.ac b/configure.ac
index ee0b1c4..d25aaac 100644
--- a/configure.ac
+++ b/configure.ac
@@ -3396,6 +3396,7 @@ WINE_CONFIG_MAKEFILE(dlls/winedmo)
 WINE_CONFIG_MAKEFILE(dlls/winegstreamer)
 WINE_CONFIG_MAKEFILE(dlls/winehid.sys)
 WINE_CONFIG_MAKEFILE(dlls/winemac.drv)
+WINE_CONFIG_MAKEFILE(dlls/wineios.drv)
 WINE_CONFIG_MAKEFILE(dlls/winemapi)
 WINE_CONFIG_MAKEFILE(dlls/wineoss.drv)
 WINE_CONFIG_MAKEFILE(dlls/wineps.drv)
diff --git a/dlls/kernel32/process.c b/dlls/kernel32/process.c
index 98313f4..4496f09 100644
--- a/dlls/kernel32/process.c
+++ b/dlls/kernel32/process.c
@@ -39,7 +39,7 @@
 
 WINE_DEFAULT_DEBUG_CHANNEL(process);
 
-static const struct _KUSER_SHARED_DATA *user_shared_data = (struct _KUSER_SHARED_DATA *)0x7ffe0000;
+static const struct _KUSER_SHARED_DATA *user_shared_data = (struct _KUSER_SHARED_DATA *)0x17ffe0000;
 
 typedef struct
 {
diff --git a/dlls/kernel32/sync.c b/dlls/kernel32/sync.c
index 5b2a848..0939959 100644
--- a/dlls/kernel32/sync.c
+++ b/dlls/kernel32/sync.c
@@ -42,7 +42,7 @@
 
 WINE_DEFAULT_DEBUG_CHANNEL(sync);
 
-static const struct _KUSER_SHARED_DATA *user_shared_data = (struct _KUSER_SHARED_DATA *)0x7ffe0000;
+static const struct _KUSER_SHARED_DATA *user_shared_data = (struct _KUSER_SHARED_DATA *)0x17ffe0000;
 
 
 static void get_create_object_attributes( OBJECT_ATTRIBUTES *attr, UNICODE_STRING *nameW,
diff --git a/dlls/kernelbase/sync.c b/dlls/kernelbase/sync.c
index 94036ac..d588594 100644
--- a/dlls/kernelbase/sync.c
+++ b/dlls/kernelbase/sync.c
@@ -39,7 +39,7 @@
 
 WINE_DEFAULT_DEBUG_CHANNEL(sync);
 
-static const struct _KUSER_SHARED_DATA *user_shared_data = (struct _KUSER_SHARED_DATA *)0x7ffe0000;
+static const struct _KUSER_SHARED_DATA *user_shared_data = (struct _KUSER_SHARED_DATA *)0x17ffe0000;
 
 /* check if current version is NT or Win95 */
 static inline BOOL is_version_nt(void)
diff --git a/dlls/ntdll/loader.c b/dlls/ntdll/loader.c
index 443b984..0801691 100644
--- a/dlls/ntdll/loader.c
+++ b/dlls/ntdll/loader.c
@@ -4435,7 +4435,7 @@ static void release_address_space(void)
  * Attach to all the loaded dlls.
  * If this is the first time, perform the full process initialization.
  */
-void loader_init( CONTEXT *context, void **entry )
+void loader_init_impl( CONTEXT *context, void **entry )
 {
     static int attach_done;
     NTSTATUS status;
@@ -4588,6 +4588,16 @@ void loader_init( CONTEXT *context, void **entry )
     RtlLeaveCriticalSection( &loader_section );
 }
 
+#if defined(__aarch64__) && defined(__WINE_PE_BUILD)
+/* JuiceWine direct loader x18 restore */
+__ASM_GLOBAL_FUNC( loader_init,
+                   "movz x18, #0x8000\n\t"
+                   "movk x18, #0xd7ff, lsl #16\n\t"
+                   "movk x18, #0x2, lsl #32\n\t"
+                   "b " __ASM_NAME("loader_init_impl") )
+#endif
+
+
 
 /***********************************************************************
  *           RtlImageDirectoryEntryToData   (NTDLL.@)
diff --git a/dlls/ntdll/signal_arm64.c b/dlls/ntdll/signal_arm64.c
index e560718..688b014 100644
--- a/dlls/ntdll/signal_arm64.c
+++ b/dlls/ntdll/signal_arm64.c
@@ -837,13 +837,26 @@ __ASM_GLOBAL_FUNC( RtlUserThreadStart,
 /******************************************************************
  *		LdrInitializeThunk (NTDLL.@)
  */
-void WINAPI LdrInitializeThunk( CONTEXT *context, ULONG_PTR unk2, ULONG_PTR unk3, ULONG_PTR unk4 )
+void WINAPI LdrInitializeThunk_impl( CONTEXT *context, ULONG_PTR unk2,
+                                      ULONG_PTR unk3, ULONG_PTR unk4 )
 {
     loader_init( context, (void **)&context->X0 );
-    TRACE_(relay)( "\1Starting thread proc %p (arg=%p)\n", (void *)context->X0, (void *)context->X1 );
+    TRACE_(relay)( "\1Starting thread proc %p (arg=%p)\n",
+                   (void *)context->X0, (void *)context->X1 );
     NtContinue( context, TRUE );
 }
 
+/*
+ * JuiceWine iOS ARM64:
+ * Reload the Windows TEB register from CONTEXT.X18 before any
+ * compiler-generated C code executes.
+ */
+/* Juice: restore Windows TEB from CONTEXT. */
+__ASM_GLOBAL_FUNC( LdrInitializeThunk,
+                   "ldr x18, [x0, #0x98]\n\t"
+                   "b " __ASM_NAME("LdrInitializeThunk_impl") )
+
+
 
 /***********************************************************************
  *           process_breakpoint
diff --git a/dlls/ntdll/thread.c b/dlls/ntdll/thread.c
index fe083be..4785565 100644
--- a/dlls/ntdll/thread.c
+++ b/dlls/ntdll/thread.c
@@ -35,7 +35,7 @@ WINE_DECLARE_DEBUG_CHANNEL(relay);
 WINE_DECLARE_DEBUG_CHANNEL(pid);
 WINE_DECLARE_DEBUG_CHANNEL(timestamp);
 
-struct _KUSER_SHARED_DATA *user_shared_data = (void *)0x7ffe0000;
+struct _KUSER_SHARED_DATA *user_shared_data = (void *)0x17ffe0000;
 
 struct debug_info
 {
diff --git a/dlls/ntdll/unix/cdrom.c b/dlls/ntdll/unix/cdrom.c
index 2ed5b5d..c5fab6d 100644
--- a/dlls/ntdll/unix/cdrom.c
+++ b/dlls/ntdll/unix/cdrom.c
@@ -27,6 +27,17 @@
 
 #include "config.h"
 
+/*
+ * Juice: Wine's Apple CD/DVD backend uses macOS-only IOKit and
+ * sys/disk.h interfaces. iOS has no exposed optical-drive backend,
+ * so use Wine's generic unsupported implementation there.
+ */
+#if defined(__APPLE__) && \
+    !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__)
+# define JUICE_MACOS_CDROM 1
+#endif
+
+
 #include <errno.h>
 #include <string.h>
 #include <stdarg.h>
@@ -75,7 +86,7 @@
 # include <sys/scsiio.h>
 #endif
 
-#ifdef __APPLE__
+#ifdef JUICE_MACOS_CDROM
 # include <libkern/OSByteOrder.h>
 # include <sys/disk.h>
 # include <IOKit/IOKitLib.h>
@@ -340,7 +351,7 @@ static int CDROM_MediaChanged(int dev)
  * This is ugly and inefficient, but we have no choice since the partition fd doesn't
  * support the eject ioctl.
  */
-#ifdef __APPLE__
+#ifdef JUICE_MACOS_CDROM
 static NTSTATUS get_parent_device( int fd, char *name, size_t len )
 {
     NTSTATUS status = STATUS_NO_SUCH_FILE;
@@ -520,7 +531,7 @@ static NTSTATUS CDROM_SyncCache(int dev, int fd)
     cdrom_cache[dev].toc_good = 1;
     return STATUS_SUCCESS;
 
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     int i;
     dk_cd_read_toc_t hdr;
     CDROM_TOC *toc = &cdrom_cache[dev].toc;
@@ -632,7 +643,7 @@ static BOOL CDROM_GetInterfaceInfo(int fd, UCHAR* iface, UCHAR* port, UCHAR* dev
         }
     }
     return FALSE;
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     dk_scsi_identify_t addr;
     if (ioctl(fd, DKIOCSCSIIDENTIFY, &addr) != -1)
     {
@@ -706,7 +717,7 @@ static NTSTATUS CDROM_GetStatusCode(int io)
  */
 static NTSTATUS CDROM_GetControl(int dev, int fd, CDROM_AUDIO_CONTROL* cac)
 {
-#ifdef __APPLE__
+#ifdef JUICE_MACOS_CDROM
     uint16_t speed;
     int io = ioctl( fd, DKIOCCDGETSPEED, &speed );
     if (io != 0) return CDROM_GetStatusCode( io );
@@ -823,7 +834,7 @@ static NTSTATUS CDROM_SetTray(int fd, BOOL doEject)
     return CDROM_GetStatusCode((ioctl(fd, CDIOCALLOW, NULL)) ||
                                (ioctl(fd, doEject ? CDIOCEJECT : CDIOCCLOSE, NULL)) ||
                                (ioctl(fd, CDIOCPREVENT, NULL)));
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     if (doEject) return CDROM_GetStatusCode( ioctl( fd, DKIOCEJECT, NULL ) );
     else return STATUS_NOT_SUPPORTED;
 #else
@@ -1096,7 +1107,7 @@ static NTSTATUS CDROM_ReadQChannel(int dev, int fd, const CDROM_SUB_Q_DATA_FORMA
 
  end:
     ret = CDROM_GetStatusCode(io);
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     SUB_Q_HEADER* hdr = (SUB_Q_HEADER*)data;
     int io = 0;
     union
@@ -1160,7 +1171,7 @@ static NTSTATUS CDROM_Verify(int dev, int fd)
         return STATUS_SUCCESS;
     else
         return STATUS_NO_MEDIA_IN_DEVICE;
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
 	/* At this point, we know that we have media, because in Mac OS X, the
 	 * device file is only created when media is present. */
 	return STATUS_SUCCESS;
@@ -1480,7 +1491,7 @@ static NTSTATUS CDROM_RawRead(int fd, const RAW_READ_INFO* raw, void* buffer, DW
 {
     int         ret = STATUS_NOT_SUPPORTED;
     int         io = -1;
-#ifdef __APPLE__
+#ifdef JUICE_MACOS_CDROM
     dk_cd_read_t cdrd;
 #endif
 
@@ -1551,7 +1562,7 @@ static NTSTATUS CDROM_RawRead(int fd, const RAW_READ_INFO* raw, void* buffer, DW
         FIXME("NIY: %d\n", raw->TrackMode);
         return STATUS_INVALID_PARAMETER;
     }
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     /* Mac OS lets us read multiple parts of the sector at a time.
      * We can read all the sectors in at once, unlike Linux.
      */
@@ -1620,7 +1631,7 @@ static NTSTATUS CDROM_ScsiPassThroughDirect(int fd, const SCSI_PASS_THROUGH_DIRE
 #ifdef HAVE_SG_IO_HDR_T_INTERFACE_ID
     sg_io_hdr_t cmd;
     int io;
-#elif defined __APPLE__
+#elif defined JUICE_MACOS_CDROM
     dk_scsi_command_t cmd;
     int io;
 #endif
@@ -1674,7 +1685,7 @@ static NTSTATUS CDROM_ScsiPassThroughDirect(int fd, const SCSI_PASS_THROUGH_DIRE
 
     ret = CDROM_GetStatusCode(io);
 
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
 
     memset(&cmd, 0, sizeof(cmd));
     memcpy(cmd.cdb, in_pkt->Cdb, in_pkt->CdbLength);
@@ -1822,7 +1833,7 @@ static NTSTATUS CDROM_ScsiPassThrough(int fd, const SCSI_PASS_THROUGH *in_pkt, S
 #ifdef HAVE_SG_IO_HDR_T_INTERFACE_ID
     sg_io_hdr_t cmd;
     int io;
-#elif defined __APPLE__
+#elif defined JUICE_MACOS_CDROM
     dk_scsi_command_t cmd;
     int io;
 #endif
@@ -1879,7 +1890,7 @@ static NTSTATUS CDROM_ScsiPassThrough(int fd, const SCSI_PASS_THROUGH *in_pkt, S
 
     ret = CDROM_GetStatusCode(io);
 
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
 
     memset(&cmd, 0, sizeof(cmd));
     memcpy(cmd.cdb, in_pkt->Cdb, in_pkt->CdbLength);
@@ -2059,7 +2070,7 @@ static NTSTATUS CDROM_ScsiGetCaps(int fd, PIO_SCSI_CAPABILITIES caps)
     caps->AdapterScansDown = FALSE; /* FIXME ? */
     caps->AdapterUsesPio = FALSE; /* FIXME ? */
     ret = STATUS_SUCCESS;
-#elif defined __APPLE__
+#elif defined JUICE_MACOS_CDROM
     uint64_t bytesr, bytesw, align;
     int io = ioctl(fd, DKIOCGETMAXBYTECOUNTREAD, &bytesr);
     if (io != 0) return CDROM_GetStatusCode(io);
@@ -2121,7 +2132,7 @@ static NTSTATUS DVD_StartSession(int fd, const DVD_SESSION_ID *sid_in, PDVD_SESS
     ret =CDROM_GetStatusCode(ioctl(fd, DVD_AUTH, &auth_info));
     *sid_out = auth_info.lsa.agid;
     return ret;
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     NTSTATUS ret = STATUS_NOT_SUPPORTED;
     dk_dvd_report_key_t dvdrk;
     DVDAuthenticationGrantIDInfo agid_info;
@@ -2157,7 +2168,7 @@ static NTSTATUS DVD_EndSession(int fd, const DVD_SESSION_ID *sid)
 
     TRACE("\n");
     return CDROM_GetStatusCode(ioctl(fd, DVD_AUTH, &auth_info));
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     dk_dvd_send_key_t dvdsk;
 
     dvdsk.format = kDVDKeyFormatAGID_Invalidate;
@@ -2206,7 +2217,7 @@ static NTSTATUS DVD_SendKey(int fd, const DVD_COPY_PROTECT_KEY *key)
 	FIXME("Unknown Keytype 0x%x\n",key->KeyType);
     }
     return ret;
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     dk_dvd_send_key_t dvdsk;
     union
     {
@@ -2341,7 +2352,7 @@ static NTSTATUS DVD_ReadKey(int fd, PDVD_COPY_PROTECT_KEY key)
 	FIXME("Unknown keytype 0x%x\n",key->KeyType);
     }
     return ret;
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     union
     {
         dk_dvd_report_key_t key;
@@ -2528,7 +2539,7 @@ static NTSTATUS DVD_GetRegion(int fd, PDVD_REGION region)
         }
     }
     return ret;
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     dk_dvd_report_key_t key;
     dk_dvd_read_structure_t dvd;
     DVDRegionPlaybackControlInfo rpc;
@@ -2708,7 +2719,7 @@ static NTSTATUS DVD_ReadStructure(int dev, const DVD_READ_STRUCTURE *structure,
     case DvdMaxDescriptor: /* Suppress warning */
 	break;
     }
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_CDROM)
     NTSTATUS ret;
     dk_dvd_read_structure_t dvdrs;
     union
@@ -2915,7 +2926,7 @@ NTSTATUS cdrom_DeviceIoControl( HANDLE device, HANDLE event, PIO_APC_ROUTINE apc
         return status;
     }
 
-#ifdef __APPLE__
+#ifdef JUICE_MACOS_CDROM
     {
         char name[100];
 
diff --git a/dlls/ntdll/unix/env.c b/dlls/ntdll/unix/env.c
index 460888b..1ba6f7a 100644
--- a/dlls/ntdll/unix/env.c
+++ b/dlls/ntdll/unix/env.c
@@ -1538,6 +1538,8 @@ static WCHAR *build_command_line( WCHAR **wargv )
  */
 static void run_wineboot( WCHAR *env, SIZE_T size )
 {
+    fprintf( stderr, "[JuiceStage] skipping wineboot for packaged prefix\n" );
+    return;
     static const WCHAR eventW[] = {'\\','K','e','r','n','e','l','O','b','j','e','c','t','s',
         '\\','_','_','w','i','n','e','b','o','o','t','_','e','v','e','n','t',0};
     static const WCHAR appnameW[] = {'\\','?','?','\\','C',':','\\','w','i','n','d','o','w','s',
diff --git a/dlls/ntdll/unix/file.c b/dlls/ntdll/unix/file.c
index b641e4c..a81f04f 100644
--- a/dlls/ntdll/unix/file.c
+++ b/dlls/ntdll/unix/file.c
@@ -26,6 +26,16 @@
 
 #include "config.h"
 
+/*
+ * Juice: distinguish desktop macOS interfaces from iOS.
+ * Some Darwin file/device constants are unavailable in the iOS SDK.
+ */
+#if defined(__APPLE__) && \
+    !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__)
+# define JUICE_MACOS_DEVICE_TYPES 1
+#endif
+
+
 #include <assert.h>
 #include <sys/types.h>
 #include <dirent.h>
@@ -2642,12 +2652,20 @@ static NTSTATUS read_directory_data_getattrlist( struct dir_data *data, const ch
         return STATUS_NO_SUCH_FILE;
     /* If unix_name named a symlink, the above may have succeeded even if the symlink is broken.
        Check that with another call without FSOPT_NOFOLLOW.  We don't ask for any attributes. */
-    if (buffer.type == VLNK)
+    /*
+     * Juice: VLNK is not exposed by the iOS SDK. Use the portable
+     * lstat/S_ISLNK test before checking whether the link target exists.
+     */
     {
-        u_int32_t dummy;
-        attrlist.commonattr = 0;
-        if (getattrlist( unix_name, &attrlist, &dummy, sizeof(dummy), 0 ) == -1)
-            return STATUS_NO_SUCH_FILE;
+        struct stat st;
+
+        if (!lstat( unix_name, &st ) && S_ISLNK( st.st_mode ))
+        {
+            u_int32_t dummy;
+            attrlist.commonattr = 0;
+            if (getattrlist( unix_name, &attrlist, &dummy, sizeof(dummy), 0 ) == -1)
+                return STATUS_NO_SUCH_FILE;
+        }
     }
 
     TRACE( "found %s\n", debugstr_a(buffer.name) );
@@ -7238,7 +7256,7 @@ NTSTATUS get_device_info( int fd, FILE_FS_DEVICE_INFORMATION *info )
             info->DeviceType = FILE_DEVICE_TAPE;
             break;
         }
-#elif defined(__FreeBSD__) || defined(__FreeBSD_kernel__) || defined(__APPLE__)
+#elif defined(__FreeBSD__) || defined(__FreeBSD_kernel__) || defined(JUICE_MACOS_DEVICE_TYPES)
         {
             int d_type;
             if (ioctl(fd, FIODTYPE, &d_type) == 0)
@@ -7250,7 +7268,7 @@ NTSTATUS get_device_info( int fd, FILE_FS_DEVICE_INFORMATION *info )
                     break;
                 case D_DISK:
                     info->DeviceType = FILE_DEVICE_DISK;
-#if defined(__APPLE__)
+#if defined(JUICE_MACOS_DEVICE_TYPES)
                     if (major(st.st_rdev) == 3 && minor(st.st_rdev) == 2) info->DeviceType = FILE_DEVICE_NULL;
 #endif
                     break;
diff --git a/dlls/ntdll/unix/loader.c b/dlls/ntdll/unix/loader.c
index c1321ce..9a95568 100644
--- a/dlls/ntdll/unix/loader.c
+++ b/dlls/ntdll/unix/loader.c
@@ -382,6 +382,7 @@ static void set_config_dir(void)
 static void init_paths(void)
 {
     Dl_info info;
+    const char *loader_override = getenv( "WINELOADER" );
 
     if (!dladdr( init_paths, &info ) || !(ntdll_dir = realpath_dirname( info.dli_fname )))
         fatal_error( "cannot get path to ntdll.so\n" );
@@ -399,6 +400,8 @@ static void init_paths(void)
         wineloader = build_path( ntdll_dir, "wine" );
     }
 
+    if (loader_override && *loader_override) wineloader = strdup( loader_override );
+
     set_dll_path();
     set_system_dll_path();
     set_home_dir();
@@ -451,6 +454,83 @@ static void preloader_exec( char **argv )
     execv( argv[0], argv );
     free( argv[0] );
 #endif
+#if defined(__APPLE__) && defined(__aarch64__)
+    /*
+     * JuiceWine: wrap nested loaders with tracing parent.
+     *
+     * Each Windows process receives a separate native Wine
+     * loader. On iOS that loader must call PT_TRACE_ME before
+     * executing anonymous PE code. Its direct parent therefore
+     * needs to be the task-port-enabled tracing helper.
+     */
+    {
+        const char *trace_parent =
+            getenv( "JUICE_WINE_TRACE_PARENT" );
+
+        if (trace_parent && trace_parent[0])
+        {
+            size_t count = 0;
+            size_t i;
+            char **wrapped;
+
+            while (argv[count + 1])
+                count++;
+
+            wrapped = malloc(
+                (count + 2) * sizeof(*wrapped)
+            );
+
+            if (!wrapped)
+                fatal_error(
+                    "out of memory creating nested "
+                    "trace-parent arguments\n"
+                );
+
+            /*
+             * Original direct exec:
+             *
+             *   argv[1] = Wine loader
+             *   argv[2...] = loader arguments
+             *
+             * Wrapped exec:
+             *
+             *   wrapped[0] = tracing helper
+             *   wrapped[1] = Wine loader
+             *   wrapped[2...] = original arguments
+             */
+            wrapped[0] = (char *)trace_parent;
+
+            for (i = 0; i < count; i++)
+                wrapped[i + 1] = argv[i + 1];
+
+            wrapped[count + 1] = NULL;
+
+            fprintf(
+                stderr,
+                "[JuiceWine] wrapping nested Wine loader "
+                "%s with %s\n",
+                argv[1],
+                trace_parent
+            );
+
+            execv(
+                trace_parent,
+                wrapped
+            );
+
+            fprintf(
+                stderr,
+                "[JuiceWine] nested trace-parent exec "
+                "failed: errno=%d (%s)\n",
+                errno,
+                strerror(errno)
+            );
+
+            free(wrapped);
+        }
+    }
+#endif
+
     execv( argv[1], argv + 1 );
 }
 
@@ -543,7 +623,20 @@ void start_server( BOOL debug )
         argv[1] = debug ? debug_flag : NULL;
         argv[2] = NULL;
         if (exec_wineserver( &pid, argv )) fatal_error( "could not exec wineserver\n" );
-        waitpid( pid, &status, 0 );
+        /*
+         * JuiceWine: traced SIGCHLD may interrupt this wait.
+         * Retry EINTR instead of interpreting an uninitialised
+         * wait status as wineserver failure.
+         */
+        while (waitpid( pid, &status, 0 ) == -1)
+        {
+            if (errno == EINTR) continue;
+            fatal_error(
+                "waitpid for wineserver failed: %s\n",
+                strerror(errno)
+            );
+        }
+
         status = WIFEXITED(status) ? WEXITSTATUS(status) : 1;
         if (status == 2) return;  /* server lock held by someone else, will retry later */
         if (status) exit(status);  /* server failed */
@@ -1865,10 +1958,69 @@ static void start_main_thread(void)
     set_load_order_app_name( main_wargv[0] );
     init_thread_stack( teb, 0, 0, 0 );
     NtCreateKeyedEvent( &keyed_event, GENERIC_READ | GENERIC_WRITE, NULL, 0 );
+    fprintf(
+        stderr,
+        "[JuiceStage] loading Windows ntdll; "
+        "transfer=%p teb=%p peb=%p\n",
+        (void *)main_image_info.TransferAddress,
+        teb,
+        peb
+    );
+    fflush( stderr );
+
     load_ntdll();
-    load_wow64_ntdll( main_image_info.Machine );
+
+    fprintf(
+        stderr,
+        "[JuiceStage] load_ntdll returned; "
+        "transfer=%p\n",
+        (void *)main_image_info.TransferAddress
+    );
+    fflush( stderr );
+
+    load_wow64_ntdll(
+        main_image_info.Machine
+    );
+
+    fprintf(
+        stderr,
+        "[JuiceStage] load_wow64_ntdll returned\n"
+    );
+    fflush( stderr );
+
+    fprintf(
+        stderr,
+        "[JuiceStage] calling load_apiset_dll\n"
+    );
+    fflush( stderr );
+
     load_apiset_dll();
+
+    fprintf(
+        stderr,
+        "[JuiceStage] load_apiset_dll returned; "
+        "stack_base=%p stack_limit=%p\n",
+        teb->Tib.StackBase,
+        teb->Tib.StackLimit
+    );
+    fflush( stderr );
+
+    fprintf(
+        stderr,
+        "[JuiceStage] calling "
+        "server_init_process_done\n"
+    );
+    fflush( stderr );
+
     server_init_process_done();
+
+    fprintf(
+        stderr,
+        "[JuiceStage] ERROR: "
+        "server_init_process_done returned "
+        "unexpectedly\n"
+    );
+    fflush( stderr );
 }
 
 
@@ -1919,13 +2071,25 @@ static void apple_main_thread(void)
     /* Multi-processing Services can get confused about the main thread if the
      * first time it's used is on a secondary thread.  Use it here to make sure
      * that doesn't happen. */
+#if !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__)
     MPTaskIsPreemptive(MPCurrentTaskID());
+#else
+    /* JUICE_IOS_SKIP_MACOS_WARMUPS:
+     * Multiprocessing Services is unavailable and unnecessary on iOS.
+     */
+#endif
 #pragma clang diagnostic pop
 
     /* Give ourselves the best chance of having the distributed notification
      * center scheduled on this thread's run loop.  In theory, it's scheduled
      * in the first thread to ask for it. */
+#if !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__)
     CFNotificationCenterGetDistributedCenter();
+#else
+    /* macOS distributed notifications are unavailable on iOS.
+     * Keep the Core Foundation run loop and thread handoff below.
+     */
+#endif
 
     /* We use this run loop source for two purposes.  First, a run loop exits
      * if it has no more sources scheduled.  So, we need at least one source
diff --git a/dlls/ntdll/unix/server.c b/dlls/ntdll/unix/server.c
index 730a0cd..a18dfe6 100644
--- a/dlls/ntdll/unix/server.c
+++ b/dlls/ntdll/unix/server.c
@@ -24,6 +24,17 @@
 
 #include "config.h"
 
+/*
+ * Juice: the Mach bootstrap task-port handoff is a desktop macOS
+ * mechanism. iOS does not expose servers/bootstrap.h, and Juice's
+ * wineserver uses the non-Mach process backend.
+ */
+#if defined(__APPLE__) && \
+    !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__)
+# define JUICE_MACOS_SERVER_TASK_PORT 1
+#endif
+
+
 #include <assert.h>
 #include <ctype.h>
 #include <errno.h>
@@ -1469,9 +1480,16 @@ static int server_connect(void)
 }
 
 
+/*
+ * Basic Mach thread APIs exist on both macOS and iOS.
+ * The bootstrap server-name API is desktop macOS-only.
+ */
 #ifdef __APPLE__
 #include <mach/mach.h>
 #include <mach/mach_error.h>
+#endif
+
+#ifdef JUICE_MACOS_SERVER_TASK_PORT
 #include <servers/bootstrap.h>
 
 /* send our task port to the server */
@@ -1516,7 +1534,7 @@ static void send_server_task_port(void)
 
     mach_port_deallocate(mach_task_self(), wineserver_port);
 }
-#endif  /* __APPLE__ */
+#endif  /* JUICE_MACOS_SERVER_TASK_PORT */
 
 
 /***********************************************************************
@@ -1744,6 +1762,13 @@ size_t server_init_process(void)
 void server_init_process_done(void)
 {
     unsigned int status;
+
+    fprintf(
+        stderr,
+        "[JuiceStage] "
+        "server_init_process_done entered\n"
+    );
+    fflush( stderr );
     FILE_FS_DEVICE_INFORMATION info;
     struct thread_data *data = get_thread_data();
     TEB64 *teb64 = get_teb64( data->teb );
@@ -1752,16 +1777,45 @@ void server_init_process_done(void)
         chdir( "/" );
     close( initial_cwd );
 
-#ifdef __APPLE__
+#ifdef JUICE_MACOS_SERVER_TASK_PORT
     send_server_task_port();
 #endif
 
     /* Install signal handlers; this cannot be done earlier, since we cannot
      * send exceptions to the debugger before the create process event that
      * is sent by init_process_done */
-    signal_init_process( data->teb );
+    fprintf(
+        stderr,
+        "[JuiceStage] calling "
+        "signal_init_process; teb=%p\n",
+        data->teb
+    );
+    fflush( stderr );
+
+    signal_init_process(
+        data->teb
+    );
+
+    fprintf(
+        stderr,
+        "[JuiceStage] "
+        "signal_init_process returned\n"
+    );
+    fflush( stderr );
+
     init_teb_data( data );
 
+    fprintf(
+        stderr,
+        "[JuiceStage] init_teb_data returned; "
+        "syscall_frame=%p stack_base=%p "
+        "stack_limit=%p\n",
+        get_syscall_frame( data ),
+        data->teb->Tib.StackBase,
+        data->teb->Tib.StackLimit
+    );
+    fflush( stderr );
+
     /* Signal the parent process to continue */
     SERVER_START_REQ( init_process_done )
     {
@@ -1772,8 +1826,41 @@ void server_init_process_done(void)
     }
     SERVER_END_REQ;
 
+    fprintf(
+        stderr,
+        "[JuiceStage] init_process_done server "
+        "call returned; status=%08x suspend=%u "
+        "transfer=%p teb=%p peb=%p\n",
+        status,
+        data->suspend,
+        (void *)main_image_info.TransferAddress,
+        data->teb,
+        peb
+    );
+    fflush( stderr );
+
     assert( !status );
-    signal_start_thread( main_image_info.TransferAddress, peb, data->teb );
+
+    fprintf(
+        stderr,
+        "[JuiceStage] calling "
+        "signal_start_thread\n"
+    );
+    fflush( stderr );
+
+    signal_start_thread(
+        main_image_info.TransferAddress,
+        peb,
+        data->teb
+    );
+
+    fprintf(
+        stderr,
+        "[JuiceStage] ERROR: "
+        "signal_start_thread returned "
+        "unexpectedly\n"
+    );
+    fflush( stderr );
 }
 
 
diff --git a/dlls/ntdll/unix/signal_arm64.c b/dlls/ntdll/unix/signal_arm64.c
index 522ade3..5ffdaeb 100644
--- a/dlls/ntdll/unix/signal_arm64.c
+++ b/dlls/ntdll/unix/signal_arm64.c
@@ -367,6 +367,19 @@ NTSTATUS signal_set_full_context( CONTEXT *context )
     if (!status && (context->ContextFlags & CONTEXT_INTEGER) == CONTEXT_INTEGER)
         frame->restore_flags |= CONTEXT_INTEGER;
 
+#ifdef __APPLE__
+    /*
+     * Juice iOS x18 fast-return workaround.
+     * Darwin's signal return clears the Windows TEB register x18.
+     * Keep the canonical TEB and force Wine's assembly return path.
+     */
+    if (!status && frame)
+    {
+        frame->x[18] = (ULONG64)data->teb;
+        syscall_frame_fixup_for_fastpath( frame );
+    }
+#endif
+
     if (is_arm64ec() && !is_ec_code( frame->pc ))
     {
         CONTEXT *user_context = (CONTEXT *)((frame->sp - sizeof(CONTEXT)) & ~15);
@@ -1100,6 +1113,19 @@ static BOOL handle_syscall_fault( struct thread_data *data, ucontext_t *context,
 }
 
 
+#ifdef __APPLE__
+/* Juice iOS x18 SIGSEGV recovery bridge */
+extern void juice_ios_restore_x18_and_retry(void);
+
+__ASM_GLOBAL_FUNC( juice_ios_restore_x18_and_retry,
+                   "mov x18, x17\n\t"
+                   "ldr x17, [x18, #0x378]\n\t"
+                   "cbz x17, 1f\n\t"
+                   "ldr x17, [x17, #0x88]\n"
+                   "1:\tbr x16" )
+#endif
+
+
 /**********************************************************************
  *		segv_handler
  *
@@ -1113,6 +1139,155 @@ static void segv_handler( int signal, siginfo_t *siginfo, void *_sigcontext )
     EXCEPTION_RECORD rec = { .ExceptionAddress = (void *)PC_sig(sigcontext) };
     DWORD64 esr = get_fault_esr( sigcontext );
 
+#ifdef __APPLE__
+    /*
+     * iOS reserves the first 4 GiB for Mach-O __PAGEZERO, so the normal
+     * Windows KUSER_SHARED_DATA address (0x7ffe0000) cannot be mapped.
+     * ARM64 applications materialise that address in a general register;
+     * redirect such a fault to Juice's equivalent mapping one 4-GiB span
+     * higher and retry the original instruction unchanged.
+     */
+    if ((ULONG_PTR)siginfo->si_addr >= 0x7ffe0000 &&
+        (ULONG_PTR)siginfo->si_addr < 0x7ffe1000 &&
+        (ESR_ELx_EC(esr) == ESR_ELx_EC_DABT_LOW ||
+         ESR_ELx_EC(esr) == ESR_ELx_EC_DABT_CUR))
+    {
+        unsigned int reg;
+        int redirected = 0;
+
+        for (reg = 0; reg <= 28; reg++)
+        {
+            ULONG_PTR value = REGn_sig(reg, sigcontext);
+
+            if (value >= 0x7ffe0000 && value < 0x7ffe1000)
+            {
+                REGn_sig(reg, sigcontext) = value + 0x100000000ULL;
+                redirected = 1;
+            }
+        }
+
+        if (redirected)
+        {
+            fprintf( stderr,
+                     "[JuiceWine] redirected KUSER_SHARED_DATA fault "
+                     "addr=%p pc=%p to 0x17ffe0000\n",
+                     siginfo->si_addr,
+                     (void *)(ULONG_PTR)PC_sig(sigcontext) );
+            return;
+        }
+    }
+    if ((ULONG_PTR)siginfo->si_addr == 0 && data->teb &&
+        !(PC_sig(sigcontext) & 3) && PC_sig(sigcontext) >= 0x20)
+    {
+        ULONG_PTR retry;
+        ULONG fault_instruction = *(const ULONG *)PC_sig(sigcontext);
+        unsigned int base_register = (fault_instruction >> 5) & 0x1f;
+
+        /*
+         * Recover compiler variants which copy x18 into a temporary
+         * register a few instructions before dereferencing it. Darwin can
+         * clear x18 across the native setjmp helper used by Wine builtins.
+         */
+        for (retry = PC_sig(sigcontext) - 4;
+             retry >= PC_sig(sigcontext) - 0x20;
+             retry -= 4)
+        {
+            ULONG instruction = *(const ULONG *)retry;
+
+            if ((instruction & 0xffffffe0) == 0xaa1203e0 &&
+                (instruction & 0x1f) == base_register)
+            {
+                REGn_sig(16, sigcontext) = retry;
+                REGn_sig(17, sigcontext) = (ULONG_PTR)data->teb;
+                PC_sig(sigcontext) =
+                    (ULONG_PTR)juice_ios_restore_x18_and_retry;
+                return;
+            }
+        }
+    }
+
+    if (!REGn_sig(18, sigcontext) && data->teb &&
+        (ULONG_PTR)siginfo->si_addr < 0x10000 &&
+        (ESR_ELx_EC(esr) == ESR_ELx_EC_DABT_LOW || ESR_ELx_EC(esr) == ESR_ELx_EC_DABT_CUR))
+    {
+        ULONG_PTR retry = PC_sig(sigcontext);
+        if ((ULONG_PTR)siginfo->si_addr == 0x1488 &&
+            REGn_sig(16, sigcontext) == retry - 0x1c) retry -= 8;
+        REGn_sig(16, sigcontext) = retry;
+        REGn_sig(17, sigcontext) = (ULONG_PTR)data->teb;
+        PC_sig(sigcontext) = (ULONG_PTR)juice_ios_restore_x18_and_retry;
+        return;
+    }
+
+    /*
+     * Darwin clears x18 when returning from a signal. If PE ntdll then
+     * faults through its missing TEB, resume through a tiny native
+     * trampoline which reloads x18 before retrying the instruction.
+     */
+    if (!REGn_sig(18, sigcontext) && data->teb &&
+        (ULONG_PTR)siginfo->si_addr < 0x10000 &&
+        (ESR_ELx_EC(esr) == ESR_ELx_EC_DABT_LOW ||
+         ESR_ELx_EC(esr) == ESR_ELx_EC_DABT_CUR) &&
+        (((PC_sig(sigcontext) & ~(ULONG_PTR)0xfffff) ==
+          ((ULONG_PTR)pLdrInitializeThunk & ~(ULONG_PTR)0xfffff)) ||
+         (!(PC_sig(sigcontext) & 3) &&
+          ((*(const ULONG *)PC_sig(sigcontext) >> 5) & 0x1f) == 18)))
+    {
+        REGn_sig(16, sigcontext) = PC_sig(sigcontext);
+        REGn_sig(17, sigcontext) = (ULONG_PTR)data->teb;
+        PC_sig(sigcontext) = (ULONG_PTR)juice_ios_restore_x18_and_retry;
+        return;
+    }
+
+    /* RtlFlsSetValue can derive x8 from x18 before faulting through x8. */
+    if (!REGn_sig(18, sigcontext) && data->teb &&
+        (ULONG_PTR)siginfo->si_addr == 0x1488 &&
+        (ESR_ELx_EC(esr) == ESR_ELx_EC_DABT_LOW || ESR_ELx_EC(esr) == ESR_ELx_EC_DABT_CUR) &&
+        !(PC_sig(sigcontext) & 3) && PC_sig(sigcontext) >= 8 &&
+        *(const ULONG *)(PC_sig(sigcontext) - 8) == 0x8b204e48 &&
+        *(const ULONG *)PC_sig(sigcontext) == 0xf90a4113 &&
+        REGn_sig(16, sigcontext) == PC_sig(sigcontext) - 0x1c)
+    {
+        REGn_sig(16, sigcontext) = PC_sig(sigcontext) - 8;
+        REGn_sig(17, sigcontext) = (ULONG_PTR)data->teb;
+        PC_sig(sigcontext) = (ULONG_PTR)juice_ios_restore_x18_and_retry;
+        return;
+    }
+#endif
+    static volatile sig_atomic_t juice_segv_count;
+    sig_atomic_t juice_number = ++juice_segv_count;
+
+    if (juice_number <= 16)
+    {
+        fprintf(
+            stderr,
+            "[JuiceStage] native segv #%d; "
+            "signal=%d code=%d pc=%p lr=%p "
+            "sp=%p x16=%p x17=%p x18=%p "
+            "addr=%p esr=%016llx ec=%02llx "
+            "teb=%p frame=%p\n",
+            (int)juice_number,
+            signal,
+            siginfo->si_code,
+            (void *)(ULONG_PTR)PC_sig(sigcontext),
+            (void *)(ULONG_PTR)LR_sig(sigcontext),
+            (void *)(ULONG_PTR)SP_sig(sigcontext),
+            (void *)(ULONG_PTR)
+                REGn_sig(16, sigcontext),
+            (void *)(ULONG_PTR)
+                REGn_sig(17, sigcontext),
+            (void *)(ULONG_PTR)
+                REGn_sig(18, sigcontext),
+            siginfo->si_addr,
+            (unsigned long long)esr,
+            (unsigned long long)
+                ESR_ELx_EC(esr),
+            data->teb,
+            get_syscall_frame( data )
+        );
+        fflush( stderr );
+    }
+
     switch (ESR_ELx_EC(esr))
     {
     case ESR_ELx_EC_IABT_LOW:
@@ -1526,6 +1701,22 @@ void init_syscall_frame( LPTHREAD_START_ROUTINE entry, void *arg, TEB *teb )
     struct thread_data *data = get_thread_data();
     struct syscall_frame *frame = get_syscall_frame( data );
     CONTEXT *ctx, context = { CONTEXT_ALL };
+    NTSTATUS juice_context_status;
+
+    fprintf(
+        stderr,
+        "[JuiceStage] init_syscall_frame entered; "
+        "entry=%p arg=%p teb=%p data=%p "
+        "frame=%p stack_base=%p stack_limit=%p\n",
+        (void *)(ULONG_PTR)entry,
+        arg,
+        teb,
+        data,
+        frame,
+        teb->Tib.StackBase,
+        teb->Tib.StackLimit
+    );
+    fflush( stderr );
     I386_CONTEXT *i386_context;
     ARM_CONTEXT *arm_context;
 
@@ -1570,18 +1761,82 @@ void init_syscall_frame( LPTHREAD_START_ROUTINE entry, void *arg, TEB *teb )
         wait_suspend( &context );
     }
 
-    ctx = (CONTEXT *)((ULONG_PTR)context.Sp & ~15) - 1;
+    ctx = (CONTEXT *)(
+        (ULONG_PTR)context.Sp & ~15
+    ) - 1;
+
     *ctx = context;
     ctx->ContextFlags = CONTEXT_FULL;
-    signal_set_full_context( ctx );
 
-    frame->sp    = (ULONG64)ctx;
+    fprintf(
+        stderr,
+        "[JuiceStage] calling "
+        "signal_set_full_context; "
+        "ctx=%p target_pc=%p target_sp=%p "
+        "target_x18=%p LdrInitializeThunk=%p "
+        "RtlUserThreadStart=%p\n",
+        ctx,
+        (void *)(ULONG_PTR)context.Pc,
+        (void *)(ULONG_PTR)context.Sp,
+        (void *)(ULONG_PTR)context.X18,
+        (void *)(ULONG_PTR)pLdrInitializeThunk,
+        (void *)(ULONG_PTR)pRtlUserThreadStart
+    );
+    fflush( stderr );
+
+    juice_context_status =
+        signal_set_full_context( ctx );
+
+    /* Juice: keep X18 in the CONTEXT for PE entry. */
+    ctx->X18 = (ULONG64)teb;
+
+    fprintf(
+        stderr,
+        "[JuiceStage] "
+        "signal_set_full_context returned; "
+        "status=%08x frame_pc=%p "
+        "frame_sp=%p frame_x18=%p "
+        "restore=%08x\n",
+        juice_context_status,
+        (void *)(ULONG_PTR)frame->pc,
+        (void *)(ULONG_PTR)frame->sp,
+        (void *)(ULONG_PTR)frame->x[18],
+        frame->restore_flags
+    );
+    fflush( stderr );
+
+    frame->sp = (ULONG64)ctx;
     frame->pc    = (ULONG64)pLdrInitializeThunk;
     frame->x[0]  = (ULONG64)ctx;
+#ifdef __APPLE__
+    frame->x[15] = (ULONG64)teb;
+#endif
     frame->x[18] = (ULONG64)teb;
-    syscall_frame_fixup_for_fastpath( frame );
-
-    pthread_sigmask( SIG_UNBLOCK, &server_block_set, NULL );
+    syscall_frame_fixup_for_fastpath(
+        frame
+    );
+
+    fprintf(
+        stderr,
+        "[JuiceStage] "
+        "init_syscall_frame complete; "
+        "dispatcher_pc=%p user_sp=%p "
+        "x16=%p x17=%p x18=%p "
+        "restore=%08x\n",
+        (void *)(ULONG_PTR)frame->pc,
+        (void *)(ULONG_PTR)frame->sp,
+        (void *)(ULONG_PTR)frame->x[16],
+        (void *)(ULONG_PTR)frame->x[17],
+        (void *)(ULONG_PTR)frame->x[18],
+        frame->restore_flags
+    );
+    fflush( stderr );
+
+    pthread_sigmask(
+        SIG_UNBLOCK,
+        &server_block_set,
+        NULL
+    );
 }
 
 
diff --git a/dlls/ntdll/unix/system.c b/dlls/ntdll/unix/system.c
index 2d6e318..5fd54b9 100644
--- a/dlls/ntdll/unix/system.c
+++ b/dlls/ntdll/unix/system.c
@@ -24,6 +24,16 @@
 
 #include "config.h"
 
+/*
+ * Juice: IOPowerSources is a desktop macOS API and its headers are
+ * absent from the iPhoneOS SDK.
+ */
+#if defined(__APPLE__) && \
+    !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__)
+# define JUICE_MACOS_POWER_SOURCES 1
+#endif
+
+
 #include <assert.h>
 #include <fcntl.h>
 #include <string.h>
@@ -64,8 +74,10 @@
 #ifdef __APPLE__
 # include <CoreFoundation/CoreFoundation.h>
 # include <IOKit/IOKitLib.h>
-# include <IOKit/ps/IOPSKeys.h>
-# include <IOKit/ps/IOPowerSources.h>
+# ifdef JUICE_MACOS_POWER_SOURCES
+#  include <IOKit/ps/IOPSKeys.h>
+#  include <IOKit/ps/IOPowerSources.h>
+# endif
 # include <mach/mach.h>
 # include <mach/machine.h>
 # include <mach/mach_init.h>
@@ -4403,7 +4415,7 @@ static NTSTATUS fill_battery_state( SYSTEM_BATTERY_STATE *bs )
     return STATUS_SUCCESS;
 }
 
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_POWER_SOURCES)
 
 static NTSTATUS fill_battery_state( SYSTEM_BATTERY_STATE *bs )
 {
@@ -4510,6 +4522,23 @@ static NTSTATUS fill_battery_state( SYSTEM_BATTERY_STATE *bs )
     return STATUS_SUCCESS;
 }
 
+#elif defined(__APPLE__)
+
+/*
+ * Juice iOS fallback. The iPhoneOS SDK does not expose the macOS
+ * IOPowerSources API. A future Juice UIKit bridge can provide the
+ * actual battery level and charging state.
+ */
+static NTSTATUS fill_battery_state( SYSTEM_BATTERY_STATE *bs )
+{
+    bs->AcOnLine = TRUE;
+    bs->BatteryPresent = FALSE;
+    bs->Charging = FALSE;
+    bs->Discharging = FALSE;
+    bs->EstimatedTime = ~0u;
+    return STATUS_SUCCESS;
+}
+
 #elif defined(__FreeBSD__)
 
 #include <dev/acpica/acpiio.h>
diff --git a/dlls/ntdll/unix/virtual.c b/dlls/ntdll/unix/virtual.c
index eda3fee..7d4886a 100644
--- a/dlls/ntdll/unix/virtual.c
+++ b/dlls/ntdll/unix/virtual.c
@@ -24,6 +24,17 @@
 
 #include "config.h"
 
+/*
+ * Juice: mach_vm.h and the mach_vm_* API are unavailable in the
+ * public iPhoneOS SDK. Keep Wine's Mach VM backend for desktop macOS
+ * and use Wine's existing mmap reservation backend on iOS.
+ */
+#if defined(__APPLE__) && \
+    !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__)
+# define JUICE_MACOS_MACH_VM 1
+#endif
+
+
 #include <assert.h>
 #include <errno.h>
 #include <fcntl.h>
@@ -66,7 +77,9 @@
 #if defined(__APPLE__)
 #define host_page_size mac_host_page_size
 # include <mach/mach_init.h>
-# include <mach/mach_vm.h>
+# ifdef JUICE_MACOS_MACH_VM
+#  include <mach/mach_vm.h>
+# endif
 # include <mach/task.h>
 # include <mach/thread_state.h>
 # include <mach/vm_map.h>
@@ -202,7 +215,7 @@ static void *host_addr_space_limit;  /* top of the host virtual address space */
 static struct file_view *arm64ec_view;
 
 ULONG_PTR user_space_wow_limit = 0;
-struct _KUSER_SHARED_DATA *user_shared_data = (void *)0x7ffe0000;
+struct _KUSER_SHARED_DATA *user_shared_data = (void *)0x17ffe0000;
 
 /* TEB allocation blocks */
 static void *teb_block;
@@ -220,6 +233,30 @@ static struct list teb_list = LIST_INIT( teb_list );
 #define MAP_NORESERVE 0
 #endif
 
+#if defined(__APPLE__) && defined(__aarch64__)
+/* JUICE_IOS_PT_TRACE_ME_DECLARATIONS */
+#ifndef PT_TRACE_ME
+#define PT_TRACE_ME 0
+#endif
+
+#define JUICE_CS_OPS_STATUS 0
+#define JUICE_CS_DEBUGGED   0x10000000U
+
+extern int ptrace(
+    int request,
+    pid_t pid,
+    char *address,
+    int signal_number
+);
+
+extern int csops(
+    pid_t pid,
+    unsigned int operation,
+    void *buffer,
+    size_t size
+);
+#endif
+
 #ifdef _WIN64  /* on 64-bit the page protection bytes use a 2-level table */
 static const size_t pages_vprot_shift = 20;
 static const size_t pages_vprot_mask = (1 << 20) - 1;
@@ -598,7 +635,7 @@ static void *anon_mmap_tryfixed( void *start, size_t size, int prot, int flags )
 #elif defined(__FreeBSD__) || defined(__FreeBSD_kernel__)
     ptr = mmap( start, size, prot, MAP_FIXED | MAP_EXCL | MAP_PRIVATE | MAP_ANON | flags, -1, 0 );
     if (ptr == MAP_FAILED && errno == EINVAL) errno = EEXIST;
-#elif defined(__APPLE__)
+#elif defined(JUICE_MACOS_MACH_VM)
     mach_vm_address_t result = (mach_vm_address_t)start;
     kern_return_t ret = mach_vm_map( mach_task_self(), &result, size, 0, VM_FLAGS_FIXED,
                                      MEMORY_OBJECT_NULL, 0, 0, prot, VM_PROT_ALL, VM_INHERIT_COPY );
@@ -628,7 +665,7 @@ static void *anon_mmap_tryfixed( void *start, size_t size, int prot, int flags )
 
 static void reserve_area( void *addr, void *end )
 {
-#ifdef __APPLE__
+#ifdef JUICE_MACOS_MACH_VM
 
 #ifdef __i386__
     static const mach_vm_address_t max_address = VM_MAX_ADDRESS;
@@ -696,7 +733,7 @@ static void reserve_area( void *addr, void *end )
         reserve_area( addr, (char *)addr + size );
         reserve_area( (char *)addr + size, end );
     }
-#endif /* __APPLE__ */
+#endif /* JUICE_MACOS_MACH_VM */
 }
 
 
@@ -865,7 +902,7 @@ static NTSTATUS load_builtin_unixlib( void *module, BOOL wow, const void **funcs
         {
             builtin->unix_handle = dlopen( builtin->unix_path, RTLD_NOW );
             if (!builtin->unix_handle)
-                WARN_(module)( "failed to load %s: %s\n", debugstr_a(builtin->unix_path), dlerror() );
+                fprintf( stderr, "[JuiceUnixlib] failed to load %s: %s\n", builtin->unix_path, dlerror() );
         }
         if (builtin->unix_handle) status = get_unixlib_funcs( builtin->unix_handle, wow, funcs, &entry );
     }
@@ -3234,6 +3271,127 @@ static NTSTATUS map_image_into_view( struct file_view *view, const UNICODE_STRIN
         }
     }
 
+#if defined(__APPLE__) && defined(__aarch64__)
+    /*
+     * JuiceWine iOS: replace executable PE sections.
+     *
+     * iOS refuses executable protection on ordinary PE
+     * file-backed mappings. Preserve the fully loaded and
+     * relocated bytes, replace the section with anonymous
+     * fixed-address memory, restore its bytes, then allow the
+     * normal Wine protection pass below to transition it to RX.
+     */
+    for (i = 0; i < nt->FileHeader.NumberOfSections; i++)
+    {
+        SIZE_T size, exec_offset;
+        void *section_addr, *exec_addr;
+        void *copy;
+
+        if (!(sec[i].Characteristics & IMAGE_SCN_MEM_EXECUTE))
+            continue;
+
+        if (sec[i].Misc.VirtualSize)
+        {
+            size = ROUND_SIZE(
+                sec[i].VirtualAddress,
+                sec[i].Misc.VirtualSize,
+                align_mask
+            );
+        }
+        else
+        {
+            size = ROUND_SIZE(
+                sec[i].VirtualAddress,
+                sec[i].SizeOfRawData,
+                align_mask
+            );
+        }
+
+        if (!size)
+            continue;
+
+        section_addr = ptr + sec[i].VirtualAddress;
+
+        /*
+         * Windows ARM64 images commonly use 4 KiB section alignment,
+         * while iOS arm64 uses 16 KiB VM pages. Replace every host page
+         * touched by the executable section, preserving neighbouring
+         * header/data bytes which may share its first or last host page.
+         */
+        exec_addr = (void *)((UINT_PTR)section_addr & ~host_page_mask);
+        exec_offset = (char *)section_addr - (char *)exec_addr;
+        if (size > ~(SIZE_T)0 - exec_offset - host_page_mask)
+        {
+            status = STATUS_INVALID_IMAGE_FORMAT;
+            goto done;
+        }
+        size = (size + exec_offset + host_page_mask) & ~host_page_mask;
+
+        if (section_addr != exec_addr || exec_offset)
+            fprintf( stderr,
+                     "[JuiceWine] host-page expanding executable section "
+                     "%.8s: section=%p mapping=%p size=%zu\n",
+                     sec[i].Name, section_addr, exec_addr, size );
+        if (!(copy = malloc(size)))
+        {
+            status = STATUS_NO_MEMORY;
+            goto done;
+        }
+
+        memcpy(
+            copy,
+            exec_addr,
+            size
+        );
+
+        if (anon_mmap_fixed(
+                exec_addr,
+                size,
+                PROT_READ | PROT_WRITE,
+                0
+            ) == MAP_FAILED)
+        {
+            fprintf(
+                stderr,
+                "[JuiceWine] anonymous replacement failed for "
+                "%s section %.8s at %p size=%zu: errno=%d (%s)\n",
+                debugstr_us(nt_name),
+                sec[i].Name,
+                exec_addr,
+                size,
+                errno,
+                strerror(errno)
+            );
+
+            free(copy);
+            status = STATUS_NO_MEMORY;
+            goto done;
+        }
+
+        memcpy(
+            exec_addr,
+            copy,
+            size
+        );
+
+        __builtin___clear_cache(
+            (char *)exec_addr,
+            (char *)exec_addr + size
+        );
+
+        free(copy);
+
+        fprintf(
+            stderr,
+            "[JuiceWine] anonymous PE executable section "
+            "%.8s at %p size=%zu\n",
+            sec[i].Name,
+            exec_addr,
+            size
+        );
+    }
+#endif
+
     /* set the image protections */
 
     set_vprot( view, ptr, ROUND_SIZE( 0, header_size, align_mask ), VPROT_COMMITTED | VPROT_READ );
@@ -4057,6 +4215,132 @@ TEB *virtual_alloc_first_teb(void)
     SIZE_T total = 32 * block_size;
     struct thread_data *thread_data;
 
+#if defined(__APPLE__) && defined(__aarch64__)
+    /*
+     * JuiceWine iOS: enable anonymous executable PE pages.
+     *
+     * Ordinary PT_ATTACH is unsupported on iOS. PT_TRACE_ME
+     * causes XNU to call cs_allow_invalid() for this process
+     * and its task-port-entitled parent.
+     */
+    {
+        static BOOL juice_trace_enabled;
+        unsigned int juice_csflags = 0;
+
+        if (!juice_trace_enabled)
+        {
+            /*
+             * A Wine process launched by an already-debugged Wine
+             * process may inherit CS_DEBUGGED across exec. Calling
+             * PT_TRACE_ME again would make it a tracee of the Wine
+             * parent, which does not run a ptrace event loop.
+             */
+            if (!csops(
+                    getpid(),
+                    JUICE_CS_OPS_STATUS,
+                    &juice_csflags,
+                    sizeof(juice_csflags)
+                ) &&
+                (juice_csflags & JUICE_CS_DEBUGGED))
+            {
+                juice_trace_enabled = TRUE;
+
+                fprintf(
+                    stderr,
+                    "[JuiceWine] inherited CS_DEBUGGED; "
+                    "PT_TRACE_ME skipped; csflags=0x%08x\n",
+                    juice_csflags
+                );
+            }
+            else
+            {
+                errno = 0;
+
+                if (ptrace( PT_TRACE_ME, 0, NULL, 0 ))
+                {
+                    fprintf(
+                        stderr,
+                        "[JuiceWine] PT_TRACE_ME failed: "
+                        "errno=%d (%s)\n",
+                        errno,
+                        strerror(errno)
+                    );
+
+                    exit(1);
+                }
+
+                juice_trace_enabled = TRUE;
+                juice_csflags = 0;
+
+                if (!csops(
+                        getpid(),
+                        JUICE_CS_OPS_STATUS,
+                        &juice_csflags,
+                        sizeof(juice_csflags)
+                    ))
+                {
+                    fprintf(
+                        stderr,
+                        "[JuiceWine] PT_TRACE_ME succeeded; "
+                        "csflags=0x%08x CS_DEBUGGED=%s\n",
+                        juice_csflags,
+                        (juice_csflags & JUICE_CS_DEBUGGED)
+                            ? "YES"
+                            : "NO"
+                    );
+                }
+                else
+                {
+                    fprintf(
+                        stderr,
+                        "[JuiceWine] PT_TRACE_ME succeeded; "
+                        "csops failed: errno=%d (%s)\n",
+                        errno,
+                        strerror(errno)
+                    );
+                }
+
+                /*
+                 * JuiceWine controlled detach handshake.
+                 *
+                 * PT_TRACE_ME has already granted CS_DEBUGGED.
+                 * Stop at a predictable point so the tracing
+                 * parent can immediately PT_DETACH. All later
+                 * signals are then handled normally by Wine.
+                 */
+                fprintf(
+                    stderr,
+                    "[JuiceWine] requesting controlled "
+                    "tracer detach\n"
+                );
+
+                raise(SIGSTOP);
+
+                juice_csflags = 0;
+
+                if (!csops(
+                        getpid(),
+                        JUICE_CS_OPS_STATUS,
+                        &juice_csflags,
+                        sizeof(juice_csflags)
+                    ))
+                {
+                    fprintf(
+                        stderr,
+                        "[JuiceWine] resumed after tracer "
+                        "detach; csflags=0x%08x "
+                        "CS_DEBUGGED=%s\n",
+                        juice_csflags,
+                        (juice_csflags & JUICE_CS_DEBUGGED)
+                            ? "YES"
+                            : "NO"
+                    );
+                }
+            }
+        }
+    }
+#endif
+
     /* reserve space for shared user data */
     status = NtAllocateVirtualMemory( NtCurrentProcess(), (void **)&user_shared_data, 0, &data_size,
                                       MEM_RESERVE | MEM_COMMIT, PAGE_READONLY );
@@ -4066,8 +4350,24 @@ TEB *virtual_alloc_first_teb(void)
         exit(1);
     }
 
-    NtAllocateVirtualMemory( NtCurrentProcess(), &teb_block, is_win64 ? limit_2g - 1 : 0, &total,
-                             MEM_RESERVE | MEM_TOP_DOWN, PAGE_READWRITE );
+#if defined(__APPLE__) && defined(__aarch64__)
+    /*
+     * iOS arm64 executables have a mandatory 4 GiB __PAGEZERO.
+     * The normal Wine limit_2g constraint therefore has no usable
+     * address and must not be used for the native 64-bit TEB block.
+     */
+    status = NtAllocateVirtualMemory( NtCurrentProcess(), &teb_block, 0, &total,
+                                      MEM_RESERVE | MEM_TOP_DOWN, PAGE_READWRITE );
+#else
+    status = NtAllocateVirtualMemory( NtCurrentProcess(), &teb_block,
+                                      is_win64 ? limit_2g - 1 : 0, &total,
+                                      MEM_RESERVE | MEM_TOP_DOWN, PAGE_READWRITE );
+#endif
+    if (status)
+    {
+        ERR( "wine: failed to allocate first TEB block: %08x\n", status );
+        exit(1);
+    }
     teb_block_pos = 30;
     ptr = (char *)teb_block + 30 * block_size;
     data_size = 2 * block_size;
diff --git a/dlls/ntoskrnl.exe/instr.c b/dlls/ntoskrnl.exe/instr.c
index 31e1e6e..a68e46f 100644
--- a/dlls/ntoskrnl.exe/instr.c
+++ b/dlls/ntoskrnl.exe/instr.c
@@ -490,7 +490,7 @@ WINE_DEFAULT_DEBUG_CHANNEL(int);
 #define SIB_BASE( sib, rex )    (((sib) & 7) | (((rex) & REX_B) ? 8 : 0))
 
 /* keep in sync with dlls/ntdll/thread.c:thread_init */
-static const BYTE *wine_user_shared_data = (BYTE *)0x7ffe0000;
+static const BYTE *wine_user_shared_data = (BYTE *)0x17ffe0000;
 static const BYTE *user_shared_data      = (BYTE *)0xfffff78000000000;
 
 static inline DWORD64 *get_int_reg( CONTEXT *context, int index )
diff --git a/dlls/win32u/Makefile.in b/dlls/win32u/Makefile.in
index 454fb25..bb83d60 100644
--- a/dlls/win32u/Makefile.in
+++ b/dlls/win32u/Makefile.in
@@ -4,7 +4,7 @@ UNIXLIB   = win32u.so
 IMPORTLIB = win32u
 IMPORTS   = ntdll
 UNIX_CFLAGS  = $(FREETYPE_CFLAGS) $(FONTCONFIG_CFLAGS)
-UNIX_LIBS    = $(CORETEXT_LIBS) $(APPKIT_LIBS) $(PTHREAD_LIBS) -lm
+UNIX_LIBS    = $(CORETEXT_LIBS) $(COREFOUNDATION_LIBS) $(PTHREAD_LIBS) -lm
 
 EXTRADLLFLAGS = -nodefaultlibs
 
diff --git a/dlls/win32u/defwnd.c b/dlls/win32u/defwnd.c
index d6852b3..f69f955 100644
--- a/dlls/win32u/defwnd.c
+++ b/dlls/win32u/defwnd.c
@@ -489,7 +489,8 @@ static LONG handle_window_pos_changing( HWND hwnd, WINDOWPOS *winpos )
     if (winpos->flags & SWP_NOSIZE) return 0;
     if ((style & WS_THICKFRAME) || ((style & (WS_POPUP | WS_CHILD)) == 0))
     {
-        MINMAXINFO info = get_min_max_info( hwnd );
+        MINMAXINFO info;
+        get_min_max_info( hwnd, &info );
         winpos->cx = min( winpos->cx, info.ptMaxTrackSize.x );
         winpos->cy = min( winpos->cy, info.ptMaxTrackSize.y );
         if (!(style & WS_MINIMIZE))
@@ -721,7 +722,7 @@ static void sys_command_size_move( HWND hwnd, WPARAM wparam )
         }
     }
 
-    minmax = get_min_max_info( hwnd );
+    get_min_max_info( hwnd, &minmax );
     dpi = get_thread_dpi();
     get_window_rect_rel( hwnd, COORDS_PARENT, &sizing_rect, dpi );
     orig_rect = sizing_rect;
diff --git a/dlls/win32u/driver.c b/dlls/win32u/driver.c
index 6270d9b..740e130 100644
--- a/dlls/win32u/driver.c
+++ b/dlls/win32u/driver.c
@@ -1367,7 +1367,7 @@ void __wine_set_user_driver( const struct user_driver_funcs *funcs, UINT version
     if (!funcs)
     {
         prev = InterlockedExchangePointer( (void **)&user_driver, (void *)&lazy_load_driver );
-        if (prev != &lazy_load_driver)
+        if (prev != &lazy_load_driver && prev != &null_user_driver)
             free( prev );
         return;
     }
@@ -1443,8 +1443,9 @@ void __wine_set_user_driver( const struct user_driver_funcs *funcs, UINT version
     SET_USER_FUNC(ThreadDetach);
 #undef SET_USER_FUNC
 
-    prev = InterlockedCompareExchangePointer( (void **)&user_driver, driver, (void *)&lazy_load_driver );
-    if (prev != &lazy_load_driver)
+    prev = InterlockedExchangePointer( (void **)&user_driver, driver );
+    fprintf( stderr, "[JuiceDriver] install prev=%p lazy=%p null=%p new=%p create=%p\n", prev, &lazy_load_driver, &null_user_driver, driver, driver->pCreateWindow );
+    if (prev != &lazy_load_driver && prev != &null_user_driver)
     {
         /* another thread beat us to it */
         free( driver );
diff --git a/dlls/win32u/menu.c b/dlls/win32u/menu.c
index a700f44..5c1483b 100644
--- a/dlls/win32u/menu.c
+++ b/dlls/win32u/menu.c
@@ -2226,6 +2226,7 @@ UINT get_menu_bar_height( HWND hwnd, UINT width, INT org_x, INT org_y )
     struct menu *menu;
     RECT rect_bar;
     HDC hdc;
+    UINT metric, height;
 
     TRACE( "hwnd %p, width %d, at (%d, %d).\n", hwnd, width, org_x, org_y );
 
@@ -2233,10 +2234,19 @@ UINT get_menu_bar_height( HWND hwnd, UINT width, INT org_x, INT org_y )
 
     hdc = NtUserGetDCEx( hwnd, 0, DCX_CACHE | DCX_WINDOW );
     NtGdiSelectFont( hdc, get_menu_font(FALSE));
-    SetRect( &rect_bar, org_x, org_y, org_x + width, org_y + get_system_metrics( SM_CYMENU ));
+    metric = get_system_metrics( SM_CYMENU );
+    if (metric < 8 || metric > 256) metric = 19;
+    SetRect( &rect_bar, org_x, org_y, org_x + width, org_y + metric );
     calc_menu_bar_size( hdc, &rect_bar, menu, hwnd );
     NtUserReleaseDC( hwnd, hdc );
-    return menu->Height;
+    height = menu->Height;
+    if (height < 8 || height > 256)
+    {
+        fprintf( stderr, "[JuiceMenu] corrected hwnd=%p width=%u height=%u->%u\n",
+                 hwnd, width, height, metric );
+        height = menu->Height = metric;
+    }
+    return height;
 }
 
 static void draw_popup_arrow( HDC hdc, RECT rect, UINT arrow_width, UINT arrow_height )
diff --git a/dlls/win32u/message.c b/dlls/win32u/message.c
index d432eb3..1789b3e 100644
--- a/dlls/win32u/message.c
+++ b/dlls/win32u/message.c
@@ -45,7 +45,7 @@ WINE_DECLARE_DEBUG_CHANNEL(relay);
 #define QS_HARDWARE     0x40000000
 #define QS_INTERNAL     (QS_DRIVER | QS_HARDWARE)
 
-static const struct _KUSER_SHARED_DATA *user_shared_data = (struct _KUSER_SHARED_DATA *)0x7ffe0000;
+static const struct _KUSER_SHARED_DATA *user_shared_data = (struct _KUSER_SHARED_DATA *)0x17ffe0000;
 
 static const struct ratio no_dpi;
 
diff --git a/dlls/win32u/sysparams.c b/dlls/win32u/sysparams.c
index fdcdfc0..06e9a11 100644
--- a/dlls/win32u/sysparams.c
+++ b/dlls/win32u/sysparams.c
@@ -4782,6 +4782,7 @@ MONITORINFO monitor_info_from_window( HWND hwnd, UINT flags )
     MONITORINFO info = {.cbSize = sizeof(info)};
     HMONITOR monitor = monitor_from_window( hwnd, flags, get_thread_dpi() );
     get_monitor_info( monitor, &info, get_thread_dpi() );
+    fprintf( stderr, "[JuiceMonitor] callee hwnd=%p hmon=%p info=%p monitor=%d,%d,%d,%d work=%d,%d,%d,%d flags=%08x\n", hwnd, monitor, &info, info.rcMonitor.left, info.rcMonitor.top, info.rcMonitor.right, info.rcMonitor.bottom, info.rcWork.left, info.rcWork.top, info.rcWork.right, info.rcWork.bottom, info.dwFlags );
     return info;
 }
 
@@ -5446,6 +5447,7 @@ static void normalize_nonclientmetrics( NONCLIENTMETRICSW *pncm)
 {
     TEXTMETRICW tm;
     HDC hdc = get_display_dc();
+    int original_border, original_caption, original_small_caption, original_menu;
 
     if( pncm->iBorderWidth < 1) pncm->iBorderWidth = 1;
     if( pncm->iCaptionWidth < 8) pncm->iCaptionWidth = 8;
@@ -5460,6 +5462,29 @@ static void normalize_nonclientmetrics( NONCLIENTMETRICSW *pncm)
     get_text_metr_size( hdc, &pncm->lfSmCaptionFont, &tm, NULL);
     pncm->iSmCaptionHeight = max( pncm->iSmCaptionHeight, 2 + tm.tmHeight);
     release_display_dc( hdc );
+
+    original_border = pncm->iBorderWidth;
+    original_caption = pncm->iCaptionHeight;
+    original_small_caption = pncm->iSmCaptionHeight;
+    original_menu = pncm->iMenuHeight;
+
+    if (pncm->iBorderWidth < 1 || pncm->iBorderWidth > 32) pncm->iBorderWidth = 1;
+    if (pncm->iPaddedBorderWidth < 0 || pncm->iPaddedBorderWidth > 32)
+        pncm->iPaddedBorderWidth = 0;
+    if (pncm->iCaptionWidth < 8 || pncm->iCaptionWidth > 256) pncm->iCaptionWidth = 18;
+    if (pncm->iCaptionHeight < 8 || pncm->iCaptionHeight > 256) pncm->iCaptionHeight = 18;
+    if (pncm->iSmCaptionWidth < 8 || pncm->iSmCaptionWidth > 256) pncm->iSmCaptionWidth = 16;
+    if (pncm->iSmCaptionHeight < 8 || pncm->iSmCaptionHeight > 256) pncm->iSmCaptionHeight = 16;
+    if (pncm->iMenuWidth < 8 || pncm->iMenuWidth > 256) pncm->iMenuWidth = 18;
+    if (pncm->iMenuHeight < 8 || pncm->iMenuHeight > 256) pncm->iMenuHeight = 18;
+    if (pncm->iScrollWidth < 8 || pncm->iScrollWidth > 256) pncm->iScrollWidth = 17;
+    if (pncm->iScrollHeight < 8 || pncm->iScrollHeight > 256) pncm->iScrollHeight = 17;
+
+    if (original_border != pncm->iBorderWidth || original_caption != pncm->iCaptionHeight ||
+        original_small_caption != pncm->iSmCaptionHeight || original_menu != pncm->iMenuHeight)
+        fprintf( stderr, "[JuiceMetrics] corrected border=%d->%d caption=%d->%d small=%d->%d menu=%d->%d\n",
+                 original_border, pncm->iBorderWidth, original_caption, pncm->iCaptionHeight,
+                 original_small_caption, pncm->iSmCaptionHeight, original_menu, pncm->iMenuHeight );
 }
 
 /* load a font (binary) parameter from the registry */
diff --git a/dlls/win32u/win32u_private.h b/dlls/win32u/win32u_private.h
index 4e2b928..a7a7069 100644
--- a/dlls/win32u/win32u_private.h
+++ b/dlls/win32u/win32u_private.h
@@ -284,7 +284,7 @@ extern HWND get_full_window_handle( HWND hwnd );
 extern HWND get_parent( HWND hwnd );
 extern HWND get_hwnd_message_parent(void);
 extern UINT get_window_dpi_awareness_context( HWND hwnd );
-extern MINMAXINFO get_min_max_info( HWND hwnd );
+extern void get_min_max_info( HWND hwnd, MINMAXINFO *result );
 extern HWND get_window_relative( HWND hwnd, UINT rel );
 extern DWORD get_window_thread( HWND hwnd, DWORD *process );
 extern HWND is_current_process_window( HWND hwnd );
diff --git a/dlls/win32u/window.c b/dlls/win32u/window.c
index 924990e..c1125d7 100644
--- a/dlls/win32u/window.c
+++ b/dlls/win32u/window.c
@@ -2249,6 +2249,7 @@ static BOOL apply_window_pos( HWND hwnd, HWND insert_after, UINT swp_flags, stru
 
     if (is_child) get_win_monitor_dpi( toplevel, &raw_dpi );
     else monitor_dpi_from_rect( new_rects->window, dpi, &raw_dpi );
+    fprintf( stderr, "[JuiceRect] input hwnd=%p dpi=%u/%u raw=%u/%u win=%d,%d,%d,%d client=%d,%d,%d,%d visible=%d,%d,%d,%d\n", hwnd, dpi.num, dpi.den, raw_dpi.num, raw_dpi.den, new_rects->window.left, new_rects->window.top, new_rects->window.right, new_rects->window.bottom, new_rects->client.left, new_rects->client.top, new_rects->client.right, new_rects->client.bottom, new_rects->visible.left, new_rects->visible.top, new_rects->visible.right, new_rects->visible.bottom );
 
     get_window_rects( hwnd, COORDS_PARENT, &old_rects, dpi );
     if (IsRectEmpty( &valid_rects[0] ) || is_layered) valid_rects = NULL;
@@ -2343,6 +2344,7 @@ static BOOL apply_window_pos( HWND hwnd, HWND insert_after, UINT swp_flags, stru
         }
     }
 
+    if (ret) fprintf( stderr, "[JuiceRect] mapped hwnd=%p win=%d,%d,%d,%d client=%d,%d,%d,%d visible=%d,%d,%d,%d\n", hwnd, monitor_rects.window.left, monitor_rects.window.top, monitor_rects.window.right, monitor_rects.window.bottom, monitor_rects.client.left, monitor_rects.client.top, monitor_rects.client.right, monitor_rects.client.bottom, monitor_rects.visible.left, monitor_rects.visible.top, monitor_rects.visible.right, monitor_rects.visible.bottom );
     release_win_ptr( win );
 
     if (ret)
@@ -4477,7 +4479,7 @@ static void send_parent_notify( HWND hwnd, UINT msg )
  *
  * Get the minimized and maximized information for a window.
  */
-MINMAXINFO get_min_max_info( HWND hwnd )
+void get_min_max_info( HWND hwnd, MINMAXINFO *result )
 {
     DWORD style = get_window_long( hwnd, GWL_STYLE );
     DWORD exstyle = get_window_long( hwnd, GWL_EXSTYLE );
@@ -4551,6 +4553,31 @@ MINMAXINFO get_min_max_info( HWND hwnd )
         }
     }
 
+    /* The arm64 iOS callback bridge can leave MINMAXINFO with pointer-shaped
+     * values. Never allow those values to become window/surface geometry. */
+    if (minmax.ptMinTrackSize.x < 0 || minmax.ptMinTrackSize.x > 4096)
+        minmax.ptMinTrackSize.x = 0;
+    if (minmax.ptMinTrackSize.y < 0 || minmax.ptMinTrackSize.y > 4096)
+        minmax.ptMinTrackSize.y = 0;
+    if (minmax.ptMaxTrackSize.x < minmax.ptMinTrackSize.x || minmax.ptMaxTrackSize.x > 32767)
+        minmax.ptMaxTrackSize.x = 32767;
+    if (minmax.ptMaxTrackSize.y < minmax.ptMinTrackSize.y || minmax.ptMaxTrackSize.y > 32767)
+        minmax.ptMaxTrackSize.y = 32767;
+    if (minmax.ptMaxSize.x < 64 || minmax.ptMaxSize.x > 32767)
+        minmax.ptMaxSize.x = 1024;
+    if (minmax.ptMaxSize.y < 64 || minmax.ptMaxSize.y > 32767)
+        minmax.ptMaxSize.y = 768;
+    if (minmax.ptMaxPosition.x < -32768 || minmax.ptMaxPosition.x > 32767)
+        minmax.ptMaxPosition.x = 0;
+    if (minmax.ptMaxPosition.y < -32768 || minmax.ptMaxPosition.y > 32767)
+        minmax.ptMaxPosition.y = 0;
+
+    fprintf( stderr, "[JuiceMinMax] hwnd=%p min=%d,%d maxtrack=%d,%d max=%d,%d pos=%d,%d\n",
+             hwnd, minmax.ptMinTrackSize.x, minmax.ptMinTrackSize.y,
+             minmax.ptMaxTrackSize.x, minmax.ptMaxTrackSize.y,
+             minmax.ptMaxSize.x, minmax.ptMaxSize.y,
+             minmax.ptMaxPosition.x, minmax.ptMaxPosition.y );
+
     TRACE( "%d %d / %d %d / %d %d / %d %d\n",
            minmax.ptMaxSize.x, minmax.ptMaxSize.y,
            minmax.ptMaxPosition.x, minmax.ptMaxPosition.y,
@@ -4561,7 +4588,7 @@ MINMAXINFO get_min_max_info( HWND hwnd )
     minmax.ptMaxTrackSize.y = max( minmax.ptMaxTrackSize.y, minmax.ptMinTrackSize.y );
 
     set_thread_dpi_awareness_context( context );
-    return minmax;
+    *result = minmax;
 }
 
 static POINT get_first_minimized_child_pos( const RECT *parent, const MINIMIZEDMETRICS *mm,
@@ -4763,7 +4790,7 @@ static UINT window_min_maximize( HWND hwnd, UINT cmd, RECT *rect )
         old_style = get_window_long( hwnd, GWL_STYLE );
         if ((old_style & WS_MAXIMIZE) && (old_style & WS_VISIBLE)) return SWP_NOSIZE | SWP_NOMOVE;
 
-        minmax = get_min_max_info( hwnd );
+        get_min_max_info( hwnd, &minmax );
 
         old_style = set_window_style_bits( hwnd, WS_MAXIMIZE, WS_MINIMIZE );
         if (old_style & WS_MINIMIZE)
@@ -4787,7 +4814,7 @@ static UINT window_min_maximize( HWND hwnd, UINT cmd, RECT *rect )
             if (win_get_flags( hwnd ) & WIN_RESTORE_MAX)
             {
                 /* Restore to maximized position */
-                minmax = get_min_max_info( hwnd );
+                get_min_max_info( hwnd, &minmax );
                 set_window_style_bits( hwnd, WS_MAXIMIZE, 0 );
                 swp_flags |= SWP_STATECHANGED;
                 SetRect( rect, minmax.ptMaxPosition.x, minmax.ptMaxPosition.y,
@@ -5729,6 +5756,7 @@ static void fix_cs_coordinates( CREATESTRUCTW *cs, INT *sw )
             return;
 
         mon_info = monitor_info_from_window( cs->hwndParent, MONITOR_DEFAULTTOPRIMARY );
+        fprintf( stderr, "[JuiceMonitor] caller hwnd=%p monitor=%d,%d,%d,%d work=%d,%d,%d,%d flags=%08x\n", cs->hwndParent, mon_info.rcMonitor.left, mon_info.rcMonitor.top, mon_info.rcMonitor.right, mon_info.rcMonitor.bottom, mon_info.rcWork.left, mon_info.rcWork.top, mon_info.rcWork.right, mon_info.rcWork.bottom, mon_info.dwFlags );
 
         if (is_default_coord( cs->x ))
         {
@@ -5805,6 +5833,7 @@ HWND WINAPI NtUserCreateWindowEx( DWORD ex_style, UNICODE_STRING *class_name,
     UINT context;
     WND *win;
 
+    fprintf( stderr, "[JuiceCreate] ex=%08x class=%p version=%p name=%p style=%08x x=%d y=%d cx=%d cy=%d parent=%p menu=%p classinst=%p params=%p flags=%08x inst=%p classptr=%p ansi=%d\n", ex_style, class_name, version, window_name, style, x, y, cx, cy, parent, menu, class_instance, params, flags, instance, class, ansi );
     TRACE( "ex_style %#x, class_name %s, version %s, window_name %s, style %#x, x %u, y %u, cx %u, cy %u, "
            "parent %p, menu %p, class_instance %p, params %p, flags %#x, instance %p, class %s, ansi %u\n",
            ex_style, debugstr_us(class_name), debugstr_us(version), debugstr_us(window_name), style, x, y, cx, cy,
@@ -5969,13 +5998,16 @@ HWND WINAPI NtUserCreateWindowEx( DWORD ex_style, UNICODE_STRING *class_name,
     cy = cs.cy;
     if ((cs.style & WS_THICKFRAME) || !(cs.style & (WS_POPUP | WS_CHILD)))
     {
-        MINMAXINFO info = get_min_max_info( hwnd );
+        MINMAXINFO info;
+        get_min_max_info( hwnd, &info );
         cx = max( min( cx, info.ptMaxTrackSize.x ), info.ptMinTrackSize.x );
         cy = max( min( cy, info.ptMaxTrackSize.y ), info.ptMinTrackSize.y );
     }
 
     if (cx < 0) cx = 0;
     if (cy < 0) cy = 0;
+    if (cx > 4096) cx = 4096;
+    if (cy > 4096) cy = 4096;
     SetRect( &new_rects.window, cs.x, cs.y, cs.x + cx, cs.y + cy );
     /* check for wraparound */
     if (cs.x > 0x7fffffff - cx) new_rects.window.right = 0x7fffffff;
diff --git a/dlls/wineios.drv/Makefile.in b/dlls/wineios.drv/Makefile.in
new file mode 100644
index 0000000..208cd95
--- /dev/null
+++ b/dlls/wineios.drv/Makefile.in
@@ -0,0 +1,11 @@
+MODULE    = wineios.drv
+UNIXLIB   = wineios.so
+IMPORTS   = user32 gdi32 win32u
+UNIX_LIBS = -lwin32u -framework CoreGraphics -framework Foundation -framework QuartzCore -framework UIKit
+
+VER_FILEDESCRIPTION_STR = "Wine iOS driver"
+
+SOURCES = \
+	dllmain.c \
+	ipc.c \
+	iosdrv.c
diff --git a/dlls/wineios.drv/dllmain.c b/dlls/wineios.drv/dllmain.c
new file mode 100644
index 0000000..af7acf7
--- /dev/null
+++ b/dlls/wineios.drv/dllmain.c
@@ -0,0 +1,12 @@
+/* Wine iOS driver entry point. LGPL-2.1-or-later. */
+#include <stdarg.h>
+#include "windef.h"
+#include "winbase.h"
+#include "wine/unixlib.h"
+
+BOOL WINAPI DllMain( HINSTANCE instance, DWORD reason, void *reserved )
+{
+    if (reason != DLL_PROCESS_ATTACH) return TRUE;
+    DisableThreadLibraryCalls( instance );
+    return !__wine_init_unix_call();
+}
diff --git a/dlls/wineios.drv/iosdrv.c b/dlls/wineios.drv/iosdrv.c
new file mode 100644
index 0000000..01e761c
--- /dev/null
+++ b/dlls/wineios.drv/iosdrv.c
@@ -0,0 +1,161 @@
+/* UIKit/software framebuffer driver for Wine on iOS. LGPL-2.1-or-later. */
+#if 0
+#pragma makedep unix
+#endif
+#include "config.h"
+#include <pthread.h>
+#include <dlfcn.h>
+#include <stdlib.h>
+#include <string.h>
+#include "iosdrv.h"
+#include <unistd.h>
+#include "ipc.h"
+#include "wine/unixlib.h"
+#include "ntstatus.h"
+WINE_DEFAULT_DEBUG_CHANNEL(iosdrv);
+
+static unsigned int screen_width=1024,screen_height=768,screen_dpi=96;
+static RECT virtual_screen={0,0,1024,768},work_area={0,0,1024,768};
+static pthread_once_t metrics_once=PTHREAD_ONCE_INIT;
+static void (*host_desktop_changed)(unsigned int,unsigned int,unsigned int);
+static void (*host_window_changed)(UINT_PTR,const RECT *,BOOL);
+static void (*host_window_destroyed)(UINT_PTR);
+static void (*host_present_bgra)(UINT_PTR,const void *,unsigned int,unsigned int,unsigned int,const RECT *);
+typedef struct { struct gdi_physdev dev; } IOSDRV_PDEVICE;
+struct iosdrv_surface { struct window_surface header; void *bits; unsigned int width,height,stride; };
+static pthread_mutex_t surface_lock=PTHREAD_MUTEX_INITIALIZER;
+static struct iosdrv_surface *active_surface;
+static struct user_driver_funcs iosdrv_funcs;
+
+static void init_metrics_once(void)
+{
+    if (host_desktop_changed) host_desktop_changed(screen_width,screen_height,screen_dpi); ios_ipc_init(screen_width,screen_height,screen_dpi);
+    TRACE("desktop %ux%u at %u dpi\n",screen_width,screen_height,screen_dpi);
+}
+static void init_metrics(void){pthread_once(&metrics_once,init_metrics_once);}
+static IOSDRV_PDEVICE *create_physdev(void){init_metrics();return calloc(1,sizeof(IOSDRV_PDEVICE));}
+static BOOL ios_CreateDC(PHYSDEV *pdev,LPCWSTR device,LPCWSTR output,const DEVMODEW *mode)
+{ IOSDRV_PDEVICE *dev=create_physdev(); if(!dev)return FALSE; push_dc_driver(pdev,&dev->dev,&iosdrv_funcs.dc_funcs); return TRUE; }
+static BOOL ios_CreateCompatibleDC(PHYSDEV orig,PHYSDEV *pdev)
+{ IOSDRV_PDEVICE *dev=create_physdev(); if(!dev)return FALSE; push_dc_driver(pdev,&dev->dev,&iosdrv_funcs.dc_funcs); return TRUE; }
+static BOOL ios_DeleteDC(PHYSDEV dev){free(dev);return TRUE;}
+static INT ios_GetDeviceCaps(PHYSDEV dev,INT cap)
+{
+    init_metrics();
+    switch(cap){
+    case HORZRES: case DESKTOPHORZRES:return screen_width;
+    case VERTRES: case DESKTOPVERTRES:return screen_height;
+    case LOGPIXELSX: case LOGPIXELSY:return screen_dpi;
+    case BITSPIXEL:return 32; case PLANES:return 1;
+    case HORZSIZE:return (screen_width*254)/(screen_dpi*10);
+    case VERTSIZE:return (screen_height*254)/(screen_dpi*10);
+    default:dev=GET_NEXT_PHYSDEV(dev,pGetDeviceCaps);return dev->funcs->pGetDeviceCaps(dev,cap);}
+}
+static LONG ios_ChangeDisplaySettings(LPDEVMODEW modes,LPCWSTR primary,HWND hwnd,DWORD flags,void *param)
+{ FIXME("display modes are controlled by iOS\n"); return DISP_CHANGE_SUCCESSFUL; }
+static UINT ios_UpdateDisplayDevices(const struct gdi_device_manager *manager,void *param)
+{
+    const DWORD flags=DISPLAY_DEVICE_ATTACHED_TO_DESKTOP|DISPLAY_DEVICE_PRIMARY_DEVICE|DISPLAY_DEVICE_VGA_COMPATIBLE;
+    struct pci_id pci={0}; struct gdi_monitor monitor={0}; DEVMODEW mode={0}; init_metrics();
+    monitor.rc_monitor=virtual_screen;monitor.rc_work=work_area;mode.dmSize=sizeof(mode);
+    mode.dmFields=DM_POSITION|DM_PELSWIDTH|DM_PELSHEIGHT|DM_BITSPERPEL|DM_DISPLAYFREQUENCY|DM_DISPLAYORIENTATION;
+    mode.dmPelsWidth=screen_width;mode.dmPelsHeight=screen_height;mode.dmBitsPerPel=32;mode.dmDisplayFrequency=60;
+    manager->add_gpu("Apple GPU",&pci,NULL,param);manager->add_source("iOS Display",flags,screen_dpi,param);
+    manager->add_monitor(&monitor,param);manager->add_modes(&mode,1,&mode,param);return STATUS_SUCCESS;
+}
+static BOOL ios_CreateDesktop(const WCHAR *name,UINT width,UINT height){init_metrics();return TRUE;}
+static void ios_SetDesktopWindow(HWND hwnd){}
+static BOOL ios_CreateWindow(HWND hwnd){init_metrics();ios_ipc_register_queue();TRACE("create %p\n",hwnd);return TRUE;}
+static void ios_DestroyWindow(HWND hwnd){if(host_window_destroyed)host_window_destroyed(HandleToUlong(hwnd));ios_ipc_destroy(hwnd);}
+static UINT ios_ShowWindow(HWND hwnd,INT cmd,RECT *rect,UINT swp)
+{
+    fprintf(stderr,"[JuiceGeom] show hwnd=%p cmd=%d rect=%p %d,%d,%d,%d swp=%x\n",hwnd,cmd,rect,rect?rect->left:0,rect?rect->top:0,rect?rect->right:0,rect?rect->bottom:0,swp);
+    if(host_window_changed)host_window_changed(HandleToUlong(hwnd),rect,cmd!=SW_HIDE);
+    ios_ipc_window(hwnd,rect,cmd!=SW_HIDE); return swp;
+}
+static BOOL ios_WindowPosChanging(HWND hwnd,UINT flags,BOOL shaped,const struct window_rects *rects){return TRUE;}
+static void ios_WindowPosChanged(HWND hwnd,HWND after,HWND owner,UINT flags,const struct window_rects *rects,struct window_surface *surface)
+{
+    fprintf(stderr,"[JuiceGeom] changed hwnd=%p flags=%x ptr=%p window=%d,%d,%d,%d client=%d,%d,%d,%d visible=%d,%d,%d,%d surface=%p\n",hwnd,flags,rects,rects->window.left,rects->window.top,rects->window.right,rects->window.bottom,rects->client.left,rects->client.top,rects->client.right,rects->client.bottom,rects->visible.left,rects->visible.top,rects->visible.right,rects->visible.bottom,surface);
+    if(host_window_changed)host_window_changed(HandleToUlong(hwnd),&rects->window,!(flags&SWP_HIDEWINDOW));
+    ios_ipc_window(hwnd,&rects->window,!(flags&SWP_HIDEWINDOW));
+}
+static void ios_SetParent(HWND hwnd,HWND parent,HWND old_parent){}
+static void ios_SetCapture(HWND hwnd,UINT flags,HWND previous){}
+static BOOL ios_ProcessEvents(DWORD mask){return ios_ipc_process_input();}
+static LRESULT ios_DesktopWindowProc(HWND hwnd,UINT msg,WPARAM wp,LPARAM lp)
+{return NtUserMessageCall(hwnd,msg,wp,lp,NULL,NtUserDefWindowProc,FALSE);}
+static LRESULT ios_WindowMessage(HWND hwnd,UINT msg,WPARAM wp,LPARAM lp){return 0;}
+
+BOOL iosdrv_present_now(HWND hwnd)
+{
+    struct iosdrv_surface *surface;
+    RECT dirty;
+    BOOL presented=FALSE;
+    pthread_mutex_lock(&surface_lock);
+    surface=active_surface;
+    if(surface&&surface->header.hwnd==hwnd)
+    {
+        SetRect(&dirty,0,0,surface->width,surface->height);
+        if(host_present_bgra)host_present_bgra(HandleToUlong(hwnd),surface->bits,surface->width,surface->height,surface->stride,&dirty);
+        ios_ipc_present(hwnd,surface->bits,surface->width,surface->height,surface->stride,&dirty);
+        presented=TRUE;
+    }
+    pthread_mutex_unlock(&surface_lock);
+    return presented;
+}
+static void surface_set_clip(struct window_surface *surface,const RECT *rects,UINT count){}
+static BOOL surface_flush(struct window_surface *header,const RECT *rect,const RECT *dirty,const BITMAPINFO *color_info,const void *color_bits,BOOL shape_changed,const BITMAPINFO *shape_info,const void *shape_bits)
+{
+    struct iosdrv_surface *surface=(struct iosdrv_surface *)header;
+    if(host_present_bgra)host_present_bgra(HandleToUlong(header->hwnd),surface->bits,surface->width,surface->height,surface->stride,dirty);
+    ios_ipc_present(header->hwnd,surface->bits,surface->width,surface->height,surface->stride,dirty);
+    return TRUE;
+}
+static void surface_destroy(struct window_surface *header)
+{
+    struct iosdrv_surface *surface=(struct iosdrv_surface *)header;
+    pthread_mutex_lock(&surface_lock);
+    if(active_surface==surface)active_surface=NULL;
+    pthread_mutex_unlock(&surface_lock);
+    free(surface->bits);
+}
+static const struct window_surface_funcs surface_funcs={surface_set_clip,surface_flush,surface_destroy};
+static BOOL ios_CreateWindowSurface(HWND hwnd,BOOL layered,const RECT *rect,struct window_surface **out)
+{
+    struct iosdrv_surface *surface; struct window_surface *header,*previous=*out;
+    D3DKMT_CREATEDCFROMMEMORY desc={.Format=D3DDDIFMT_A8R8G8B8}; BITMAPINFO info; HBITMAP bitmap=0;
+    unsigned int width=max(1,rect->right-rect->left),height=max(1,rect->bottom-rect->top),stride=width*4; void *bits;
+    ios_ipc_register_queue();
+    fprintf(stderr,"[JuiceGeom] surface hwnd=%p rect=%d,%d,%d,%d size=%ux%u previous=%p\n",hwnd,rect->left,rect->top,rect->right,rect->bottom,width,height,previous);
+    if(previous&&previous->funcs==&surface_funcs)return TRUE; if(!(bits=calloc(height,stride)))return FALSE;
+    memset(&info,0,sizeof(info));info.bmiHeader.biSize=sizeof(info.bmiHeader);info.bmiHeader.biWidth=width;
+    info.bmiHeader.biHeight=-(LONG)height;info.bmiHeader.biPlanes=1;info.bmiHeader.biBitCount=32;
+    info.bmiHeader.biCompression=BI_RGB;info.bmiHeader.biSizeImage=stride*height;
+    desc.Width=width;desc.Height=height;desc.Pitch=stride;desc.pMemory=bits;desc.hDeviceDc=NtUserGetDCEx(hwnd,0,DCX_CACHE|DCX_WINDOW);
+    if(!NtGdiDdDDICreateDCFromMemory(&desc)){bitmap=desc.hBitmap;NtGdiDeleteObjectApp(desc.hDc);}
+    else ERR("failed to wrap iOS framebuffer in GDI bitmap\n");
+    if(desc.hDeviceDc)NtUserReleaseDC(hwnd,desc.hDeviceDc);
+    header=window_surface_create(sizeof(*surface),&surface_funcs,hwnd,rect,&info,bitmap);
+    if(!header){if(bitmap)NtGdiDeleteObjectApp(bitmap);free(bits);return FALSE;}
+    surface=(struct iosdrv_surface *)header;surface->bits=bits;surface->width=width;surface->height=height;surface->stride=stride;
+    pthread_mutex_lock(&surface_lock);active_surface=surface;pthread_mutex_unlock(&surface_lock);
+    if(previous)window_surface_release(previous);*out=header;return TRUE;
+}
+static UINT ios_OpenGLInit(UINT version,const struct opengl_funcs *funcs,const struct opengl_driver_funcs **driver)
+{FIXME("OpenGL unavailable; Metal translation backend required\n");return STATUS_NOT_SUPPORTED;}
+static UINT ios_VulkanInit(UINT version,void *handle,const struct vulkan_driver_funcs **driver)
+{FIXME("Vulkan unavailable until a MoltenVK bridge is provided\n");return STATUS_NOT_SUPPORTED;}
+static struct user_driver_funcs iosdrv_funcs={
+    .dc_funcs.pCreateCompatibleDC=ios_CreateCompatibleDC,.dc_funcs.pCreateDC=ios_CreateDC,
+    .dc_funcs.pDeleteDC=ios_DeleteDC,.dc_funcs.pGetDeviceCaps=ios_GetDeviceCaps,.dc_funcs.priority=GDI_PRIORITY_GRAPHICS_DRV,
+    .pChangeDisplaySettings=ios_ChangeDisplaySettings,.pUpdateDisplayDevices=ios_UpdateDisplayDevices,
+    .pCreateDesktop=ios_CreateDesktop,.pCreateWindow=ios_CreateWindow,.pSetDesktopWindow=ios_SetDesktopWindow,
+    .pDesktopWindowProc=ios_DesktopWindowProc,.pDestroyWindow=ios_DestroyWindow,.pProcessEvents=ios_ProcessEvents,
+    .pSetCapture=ios_SetCapture,.pSetParent=ios_SetParent,.pShowWindow=ios_ShowWindow,.pWindowMessage=ios_WindowMessage,
+    .pWindowPosChanging=ios_WindowPosChanging,.pCreateWindowSurface=ios_CreateWindowSurface,
+    .pWindowPosChanged=ios_WindowPosChanged,.pOpenGLInit=ios_OpenGLInit,.pVulkanInit=ios_VulkanInit};
+NTSTATUS __wine_unix_lib_init(void)
+{host_desktop_changed=dlsym(RTLD_DEFAULT,"wineios_host_desktop_changed");host_window_changed=dlsym(RTLD_DEFAULT,"wineios_host_window_changed");host_window_destroyed=dlsym(RTLD_DEFAULT,"wineios_host_window_destroyed");host_present_bgra=dlsym(RTLD_DEFAULT,"wineios_host_present_bgra");
+init_metrics();iosdrv_funcs.pCreateWindow=ios_CreateWindow;iosdrv_funcs.pCreateDesktop=ios_CreateDesktop;iosdrv_funcs.pCreateWindowSurface=ios_CreateWindowSurface;fprintf(stderr,"[JuiceDriver] ios init set driver create=%p size=%zu\n",iosdrv_funcs.pCreateWindow,sizeof(iosdrv_funcs));__wine_set_user_driver(&iosdrv_funcs,WINE_GDI_DRIVER_VERSION);TRACE("Wine iOS driver initialized\n");return STATUS_SUCCESS;
+}
diff --git a/dlls/wineios.drv/iosdrv.h b/dlls/wineios.drv/iosdrv.h
new file mode 100644
index 0000000..773f776
--- /dev/null
+++ b/dlls/wineios.drv/iosdrv.h
@@ -0,0 +1,15 @@
+#ifndef __WINE_IOSDRV_H
+#define __WINE_IOSDRV_H
+#include "windef.h"
+#include "winbase.h"
+#include "ntgdi.h"
+#include "ntuser.h"
+#include "wine/gdi_driver.h"
+#include "wine/debug.h"
+/* Optional Juice host ABI. Pixels are temporary, top-down BGRA8. */
+extern void wineios_host_desktop_changed(unsigned int,unsigned int,unsigned int) __attribute__((weak_import));
+extern void wineios_host_window_changed(UINT_PTR,const RECT *,BOOL) __attribute__((weak_import));
+extern void wineios_host_window_destroyed(UINT_PTR) __attribute__((weak_import));
+extern void wineios_host_present_bgra(UINT_PTR,const void *,unsigned int,unsigned int,unsigned int,const RECT *) __attribute__((weak_import));
+BOOL iosdrv_present_now(HWND hwnd);
+#endif
diff --git a/dlls/wineios.drv/ipc.c b/dlls/wineios.drv/ipc.c
new file mode 100644
index 0000000..0227695
--- /dev/null
+++ b/dlls/wineios.drv/ipc.c
@@ -0,0 +1,207 @@
+/* Juice cross-process display transport. LGPL-2.1-or-later. */
+#if 0
+#pragma makedep unix
+#endif
+#include "config.h"
+#include <errno.h>
+#include <fcntl.h>
+#include <pthread.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/socket.h>
+#include <sys/un.h>
+#include <unistd.h>
+#include "ipc.h"
+#include "wine/server.h"
+WINE_DEFAULT_DEBUG_CHANNEL(iosdrv);
+static int ipc_fd=-1; static pthread_mutex_t ipc_lock=PTHREAD_MUTEX_INITIALIZER;
+static __thread BOOL queue_registered;
+static HWND input_target;
+static BOOL write_all(int fd,const void *data,size_t size){const char *p=data;while(size){ssize_t n=write(fd,p,size);if(n<0&&errno==EINTR)continue;if(n<=0)return FALSE;p+=n;size-=n;}return TRUE;}
+static BOOL read_all(int fd,void *data,size_t size){char *p=data;while(size){ssize_t n=read(fd,p,size);if(n<0&&errno==EINTR)continue;if(n<=0)return FALSE;p+=n;size-=n;}return TRUE;}
+static void send_msg(UINT type,HWND hwnd,const RECT *rect,const void *payload,UINT size,UINT stride,UINT flags)
+{
+ struct juice_ios_msg msg={JUICE_IOS_MAGIC,type,size,(UINT64)(UINT_PTR)hwnd};
+ if(rect){msg.x=rect->left;msg.y=rect->top;msg.width=rect->right-rect->left;msg.height=rect->bottom-rect->top;} msg.stride=stride;msg.flags=flags;
+ pthread_mutex_lock(&ipc_lock);if(ipc_fd>=0&&(!write_all(ipc_fd,&msg,sizeof(msg))||(size&&!write_all(ipc_fd,payload,size)))){close(ipc_fd);ipc_fd=-1;}pthread_mutex_unlock(&ipc_lock);
+}
+BOOL ios_ipc_process_input(void)
+{
+ for(;;)
+ {
+  struct juice_ios_msg msg;
+  INPUT input={0};
+  void *payload=NULL;
+  HWND hwnd,target;
+  RECT window;
+  UINT sent=0,units=0,i;
+  LRESULT text_length=0;
+  BOOL redrawn=FALSE,presented=FALSE;
+  ssize_t available;
+
+  if(ipc_fd<0) break;
+  available=recv(ipc_fd,&msg,sizeof(msg),MSG_PEEK|MSG_DONTWAIT);
+  if(available<(ssize_t)sizeof(msg)) break;
+  if(!read_all(ipc_fd,&msg,sizeof(msg)))
+  {
+   close(ipc_fd);
+   ipc_fd=-1;
+   break;
+  }
+  if(msg.size>64u*1024u)
+  {
+   fprintf(stderr,"[JuiceInput] rejected oversized message type=%u size=%u\n",msg.type,msg.size);
+   close(ipc_fd);
+   ipc_fd=-1;
+   break;
+  }
+  if(msg.size)
+  {
+   payload=malloc(msg.size);
+   if(!payload||!read_all(ipc_fd,payload,msg.size))
+   {
+    free(payload);
+    close(ipc_fd);
+    ipc_fd=-1;
+    break;
+   }
+  }
+  if(msg.magic!=JUICE_IOS_MAGIC)
+  {
+   fprintf(stderr,"[JuiceInput] ignored invalid magic=%08x type=%u size=%u\n",msg.magic,msg.type,msg.size);
+   free(payload);
+   continue;
+  }
+
+  hwnd=(HWND)(UINT_PTR)msg.hwnd;
+  if(msg.type==JUICE_IOS_INPUT&&!msg.size)
+  {
+   input.type=INPUT_MOUSE;
+   input.mi.dx=msg.x;
+   input.mi.dy=msg.y;
+   if(NtUserGetWindowRect(hwnd,&window,NtUserGetDpiForWindow(hwnd)))
+   {
+    input.mi.dx+=window.left;
+    input.mi.dy+=window.top;
+   }
+   target=NtUserWindowFromPoint(input.mi.dx,input.mi.dy);
+   if(!target) target=hwnd;
+   if(msg.flags&(JUICE_IOS_LEFT_DOWN|JUICE_IOS_RIGHT_DOWN))
+   {
+    NtUserSetForegroundWindow(hwnd);
+    NtUserSetActiveWindow(hwnd);
+    NtUserSetFocus(target);
+    input_target=target;
+   }
+   input.mi.dwFlags=MOUSEEVENTF_ABSOLUTE|MOUSEEVENTF_MOVE|MOUSEEVENTF_MOVE_NOCOALESCE;
+   if(msg.flags&JUICE_IOS_LEFT_DOWN) input.mi.dwFlags|=MOUSEEVENTF_LEFTDOWN;
+   if(msg.flags&JUICE_IOS_LEFT_UP) input.mi.dwFlags|=MOUSEEVENTF_LEFTUP;
+   if(msg.flags&JUICE_IOS_RIGHT_DOWN) input.mi.dwFlags|=MOUSEEVENTF_RIGHTDOWN;
+   if(msg.flags&JUICE_IOS_RIGHT_UP) input.mi.dwFlags|=MOUSEEVENTF_RIGHTUP;
+   sent=NtUserSendHardwareInput(target,0,&input,0);
+   fprintf(stderr,"[JuiceInput] dispatched surface=%p target=%p local=%d,%d desktop=%d,%d flags=%x sent=%u\n",
+           hwnd,target,msg.x,msg.y,input.mi.dx,input.mi.dy,input.mi.dwFlags,sent);
+  }
+  else if(msg.type==JUICE_IOS_TEXT&&msg.size&&!(msg.size%sizeof(WCHAR)))
+  {
+   target=input_target?input_target:hwnd;
+   NtUserSetForegroundWindow(hwnd);
+   NtUserSetActiveWindow(hwnd);
+   NtUserSetFocus(target);
+   units=msg.size/sizeof(WCHAR);
+   for(i=0;i<units;i++)
+   {
+    NtUserMessageCall(target,WM_CHAR,((WCHAR *)payload)[i],1,NULL,NtUserSendMessage,FALSE);
+    sent++;
+   }
+   text_length=NtUserMessageCall(target,WM_GETTEXTLENGTH,0,0,NULL,NtUserSendMessage,FALSE);
+   redrawn=NtUserRedrawWindow(target,NULL,0,RDW_INVALIDATE|RDW_UPDATENOW|RDW_ALLCHILDREN);
+   redrawn|=NtUserRedrawWindow(hwnd,NULL,0,RDW_INVALIDATE|RDW_UPDATENOW|RDW_ALLCHILDREN);
+   presented=iosdrv_present_now(hwnd);
+   fprintf(stderr,"[JuiceInput] text surface=%p target=%p utf16_units=%u delivered=%u length=%ld redraw=%u present=%u\n",hwnd,target,units,sent,(long)text_length,redrawn,presented);
+  }
+  else if(msg.type==JUICE_IOS_KEY&&!msg.size&&(msg.flags&0xffffu))
+  {
+   target=input_target?input_target:hwnd;
+   NtUserSetForegroundWindow(hwnd);
+   NtUserSetActiveWindow(hwnd);
+   NtUserSetFocus(target);
+   NtUserMessageCall(target,WM_CHAR,msg.flags&0xffffu,1,NULL,NtUserSendMessage,FALSE);
+   sent=1;
+   text_length=NtUserMessageCall(target,WM_GETTEXTLENGTH,0,0,NULL,NtUserSendMessage,FALSE);
+   redrawn=NtUserRedrawWindow(target,NULL,0,RDW_INVALIDATE|RDW_UPDATENOW|RDW_ALLCHILDREN);
+   redrawn|=NtUserRedrawWindow(hwnd,NULL,0,RDW_INVALIDATE|RDW_UPDATENOW|RDW_ALLCHILDREN);
+   presented=iosdrv_present_now(hwnd);
+   fprintf(stderr,"[JuiceInput] key surface=%p target=%p vk=%x delivered=%u length=%ld redraw=%u present=%u\n",hwnd,target,msg.flags&0xffffu,sent,(long)text_length,redrawn,presented);
+  }
+  else
+  {
+   fprintf(stderr,"[JuiceInput] ignored invalid message type=%u size=%u flags=%x\n",
+           msg.type,msg.size,msg.flags);
+  }
+  free(payload);
+ }
+ return TRUE;
+}
+void ios_ipc_register_queue(void)
+{
+ HANDLE handle;
+ int ret;
+
+ if(queue_registered||ipc_fd<0) return;
+ if(wine_server_fd_to_handle(ipc_fd,GENERIC_READ|SYNCHRONIZE,0,&handle))
+ {
+  fprintf(stderr,"[JuiceInput] failed to allocate queue fd handle tid=%p\n",NtCurrentTeb()->ClientId.UniqueThread);
+  return;
+ }
+ SERVER_START_REQ(set_queue_fd)
+ {
+  req->handle=wine_server_obj_handle(handle);
+  ret=wine_server_call(req);
+ }
+ SERVER_END_REQ;
+ NtClose(handle);
+ if(ret) fprintf(stderr,"[JuiceInput] failed to register queue fd status=%x tid=%p\n",ret,NtCurrentTeb()->ClientId.UniqueThread);
+ else
+ {
+  queue_registered=TRUE;
+  fprintf(stderr,"[JuiceInput] queue fd registered fd=%d tid=%p\n",ipc_fd,NtCurrentTeb()->ClientId.UniqueThread);
+ }
+}
+void ios_ipc_init(unsigned int width,unsigned int height,unsigned int dpi)
+{
+ const char *path=getenv("JUICE_IOS_SOCKET");
+ struct sockaddr_un addr;
+ struct juice_ios_msg hello={JUICE_IOS_MAGIC,JUICE_IOS_HELLO,0,0,0,0,(INT)width,(INT)height,dpi,(UINT)getpid()};
+
+ if(!path||!*path) return;
+ memset(&addr,0,sizeof(addr));
+ addr.sun_family=AF_UNIX;
+ if(strlen(path)>=sizeof(addr.sun_path)) return;
+ strcpy(addr.sun_path,path);
+ ipc_fd=socket(AF_UNIX,SOCK_STREAM,0);
+ if(ipc_fd<0||connect(ipc_fd,(struct sockaddr *)&addr,sizeof(addr))<0)
+ {
+  if(ipc_fd>=0) close(ipc_fd);
+  ipc_fd=-1;
+  return;
+ }
+ if(!write_all(ipc_fd,&hello,sizeof(hello)))
+ {
+  close(ipc_fd);
+  ipc_fd=-1;
+  return;
+ }
+ ios_ipc_register_queue();
+}
+void ios_ipc_window(HWND hwnd,const RECT *rect,BOOL visible){send_msg(JUICE_IOS_WINDOW,hwnd,rect,NULL,0,0,visible);}
+void ios_ipc_destroy(HWND hwnd){send_msg(JUICE_IOS_DESTROY,hwnd,NULL,NULL,0,0,0);}
+void ios_ipc_present(HWND hwnd,const void *bits,unsigned int width,unsigned int height,unsigned int stride,const RECT *dirty)
+{
+ struct juice_ios_msg msg={JUICE_IOS_MAGIC,JUICE_IOS_FRAME,stride*height,(UINT64)(UINT_PTR)hwnd,
+                           dirty->left,dirty->top,width,height,stride,0};
+ pthread_mutex_lock(&ipc_lock);
+ if(ipc_fd>=0&&(!write_all(ipc_fd,&msg,sizeof(msg))||!write_all(ipc_fd,bits,msg.size)))
+ {close(ipc_fd);ipc_fd=-1;}
+ pthread_mutex_unlock(&ipc_lock);
+}
diff --git a/dlls/wineios.drv/ipc.h b/dlls/wineios.drv/ipc.h
new file mode 100644
index 0000000..13cb145
--- /dev/null
+++ b/dlls/wineios.drv/ipc.h
@@ -0,0 +1,24 @@
+#ifndef __WINE_IOS_IPC_H
+#define __WINE_IOS_IPC_H
+#include "iosdrv.h"
+#define JUICE_IOS_MAGIC 0x4a554943u
+#define JUICE_IOS_HELLO 1u
+#define JUICE_IOS_DESKTOP 2u
+#define JUICE_IOS_WINDOW 3u
+#define JUICE_IOS_DESTROY 4u
+#define JUICE_IOS_FRAME 5u
+#define JUICE_IOS_INPUT 100u
+#define JUICE_IOS_TEXT 101u
+#define JUICE_IOS_KEY 102u
+#define JUICE_IOS_LEFT_DOWN 1u
+#define JUICE_IOS_LEFT_UP 2u
+#define JUICE_IOS_RIGHT_DOWN 4u
+#define JUICE_IOS_RIGHT_UP 8u
+struct juice_ios_msg { UINT magic,type,size; UINT64 hwnd; INT x,y,width,height; UINT stride,flags; };
+void ios_ipc_init(unsigned int width,unsigned int height,unsigned int dpi);
+void ios_ipc_register_queue(void);
+void ios_ipc_window(HWND hwnd,const RECT *rect,BOOL visible);
+void ios_ipc_destroy(HWND hwnd);
+void ios_ipc_present(HWND hwnd,const void *bits,unsigned int width,unsigned int height,unsigned int stride,const RECT *dirty);
+BOOL ios_ipc_process_input(void);
+#endif
diff --git a/server/process.h b/server/process.h
index dff766d..158ffaf 100644
--- a/server/process.h
+++ b/server/process.h
@@ -126,7 +126,11 @@ extern void set_process_base_priority( struct process *process, int base_priorit
 extern struct thread *console_get_renderer( struct console *console );
 
 /* process tracing mechanism to use */
-#ifdef __APPLE__
+/*
+ * Juice: Wine's Mach tracing backend requires macOS-only APIs.
+ * Use ptrace on iOS while retaining Mach on macOS.
+ */
+#if defined(__APPLE__) && !defined(__ENVIRONMENT_IPHONE_OS_VERSION_MIN_REQUIRED__)
 #define USE_MACH
 #elif defined(__sun)
 #define USE_PROCFS
diff --git a/tools/winebuild/res32.c b/tools/winebuild/res32.c
index 798dee7..bdf9ee2 100644
--- a/tools/winebuild/res32.c
+++ b/tools/winebuild/res32.c
@@ -196,12 +196,40 @@ static void load_next_resource( DLLSPEC *spec, const char *name )
 int load_res32_file( const char *name, DLLSPEC *spec )
 {
     int ret;
+    char full_name[PATH_MAX];
+    const char *resource_name = name;
+
+    /*
+     * The generated assembly is created in a temporary directory.
+     * Relative .incbin paths would therefore be resolved against that
+     * temporary directory instead of the Wine build directory.
+     */
+    if (name[0] != '/')
+    {
+        const char *pwd = getenv( "PWD" );
+        int len;
+
+        /*
+         * Preserve the logical rootless-jailbreak path under /var/jb.
+         * realpath() converts it into /private/preboot/.../jb, which
+         * the target Clang assembler cannot access in this environment.
+         */
+        if (!pwd || pwd[0] != '/')
+            fatal_error( "PWD is unavailable while resolving resource %s\n", name );
+
+        len = snprintf( full_name, sizeof(full_name), "%s/%s", pwd, name );
+        if (len < 0 || (size_t)len >= sizeof(full_name))
+            fatal_error( "Resource path is too long: %s\n", name );
+
+        resource_name = full_name;
+    }
 
-    init_input_buffer( name );
+    init_input_buffer( resource_name );
 
     if ((ret = check_header()))
     {
-        while (input_buffer_pos < input_buffer_size) load_next_resource( spec, name );
+        while (input_buffer_pos < input_buffer_size)
+            load_next_resource( spec, resource_name );
     }
     return ret;
 }
